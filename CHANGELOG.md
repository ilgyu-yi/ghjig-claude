# Changelog

All notable changes to this project are recorded here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Version numbers follow [semver](https://semver.org/) 0.x (`MAJOR=0` throughout v0; see SPEC §3.5 and §18.4). The per-PR fragment contract for unreleased changes lives at [`changelog_unreleased/TEMPLATE.md`](changelog_unreleased/TEMPLATE.md); `/release X.Y.Z` consolidates the fragments into a new section here.

## [0.7.0] — 2026-07-19

### Added

- `/recall --deep` adds a deep tier that searches issue/PR comment bodies (open + closed candidates) with a token-aware fixed-string match, surfacing comment-borne decisions the light tier misses (e.g. an open issue's comment thread); it runs only on explicit user intent, never the pre-planning reflex, and stays pointers-only. (#524)
- An advisory readability / language-idiom review axis: a per-language rubric (`.claude/rubrics/bash.md`) conditionally read into `code-reviewer` when the diff contains that language, plus the born-advisory `scripts/lint_bash_idioms.sh` deterministic checker (reuses the pinned shellcheck at a broader-than-gate severity, adds only the `safe_source` and `git add -A/-u` project-policy greps). Advisory by construction — never a block, never wired into the fail-closed CI lint gate. (#546)
- A new `merge-completeness` advisory arm on `gh pr merge` (immediately after `merge-attestation`, same entry grep): on a `feat`/`fix` PR whose merge diff touches zero source files (non-empty file list, every path test/doc per the reused `.shellsecretignore` classifier) it emits an `audit_log warn merge-completeness` record plus a one-line stderr notice, then allows — the positive-completeness face of the merge-attestation block. It reuses the hardened merge argv parser and a single bounded `gh pr view --json headRefName,title,files` round-trip, and NEVER blocks: fail-open throughout (PR unresolvable / gh down / empty diff / helper miss → no warn, allow). (#548)
- Reviewer contest now runs a mandatory-invariant preservation gate before the axis contest (`plan-reviewer`) and the evidence judgment (`activation-reviewer`): a candidate that defers, weakens, or measurement-gates a declared invariant is disqualified regardless of axis score, `/work-on` threads a dispatch-derived `Mandatory invariants` manifest, and an empty manifest resolves by affirmative attestation rather than a hard block. (#569)
- The `plan-reviewer` contest now carries a judge-side **dismissal evidence-burden** guard — the mirror of the challenger-side `fake-diff` / `minimalism-by-deferral` guards applied to the judge's own verdict: an `A-stands` against a domination claim is valid only when the judge *refutes* the challenger's specific weakness with evidence (scaled to its claimed magnitude), never merely *outweighs* it with incumbent advocacy. It closes the reclassification-to-dodge and unevidenced-down-rate escape hatches and fires only on a domination claim, so a large weakness can no longer be buried as an "acceptable trade-off" while benign incumbent-presumption is preserved. (#571)
- `issue-reviewer` gains an advisory phase-slice **Check 6** that flags a proposed Issue that is a single doc-only / test-only / code-only slice of a larger multi-phase change whose sibling phases would be filed as *separate* issues — the issue-level corollary now explicit in SPEC §1.2 (one change = one Execution Issue; Doc/Test/Code phases are *commits*, not issues). It is advisory only: it surfaces as a report NOTE, never escalates to `block`, and leaves the `ship`/`refine`/`block` verdict grammar unchanged; legitimately single-surface changes (pure-docs / terminal-artifact ADR / test-only hardening / refactor) and dir-mode Directives that spawn multiple Execution Issues are not flagged. (#579)
- A Python readability/language-idiom rubric (`.claude/rubrics/python.md`) — the second language on the #546 per-language mechanism. `code-reviewer` conditionally reads it when a diff contains Python and applies it as advisory idiom-notes (never a block): EAFP/duck typing, comprehensions & generators, context managers, idiomatic iteration, `dataclass`/`NamedTuple`/`Enum` structured data, type hints as contracts, and the `type-by-attribute-combination` design smell (dispatch on the explicit discriminator). PEP 8 / unused-imports / mutable-default / bare-`except` are noted as delegated to ruff/flake8; a Python deterministic checker script is deferred (no Python code / no pinned linter in this repo). (#581)
- SPEC §9.2 (issue template spec) gains an issue-**title** principle: the title states the need as explicitly and simply as possible (a plain problem statement, the primary recall/triage index into the issue as durable-memory), and is **not** a `<type>(#N):` commit/PR subject (§10.2 / §10.3) — that form conflates the change with the problem and a leading `#N` reads as a spurious parent-directive indicator. Stated as a guiding norm, not a rigid format or a hard lint/gate; `.claude/templates/issue.md` carries a one-line thin-pointer hint. (#583)
- `/file-review <pr>` (SPEC §5.29) — a producer command that runs `code-reviewer` on a PR and materializes its verdict as a first-class, `commit_id`-pinned GitHub review submitted via REST (`gh api …/pulls/<n>/reviews`, not `gh pr review`, so a concurrent push cannot rebind an approve to an unreviewed head). Ownership-branches: another author's PR → native `APPROVE`/`REQUEST_CHANGES`; the authed user's own PR → a `COMMENT` review carrying the machine-readable marker `<!-- file-review verdict=<approve|block> head=<HEAD_SHA> reviewer=code-reviewer -->` (GitHub 422s a self approve/request-changes). Maps `ship`→approve and `ship after fix`/`block`→block; an unconfirmed/unresolvable head posts nothing and audits `invalid` rather than a block on a head it couldn't confirm. Body is passed via a written, `@mention`-neutralized temp file; `<pr>` is validated and the PR must target the origin repo (cross-repo confirmed in `attended`, refused in `unattended`). Producer-only — it changes no merge gate; the #586 merge-review gate will consume it. (#585)
- Unattended `/ship` now reaches merge on a clean PR (SPEC §5.7.1) — a narrow `permissions.allow` for the exact merge form `Bash(gh pr merge --auto --merge --delete-branch)` makes Claude Code's auto-mode classifier defer that one command to the shell's own merge-review gate (#586), which posts the head-pinned review inside `/ship` when `review-gate=required`. The deferral is coupled to `review-gate=required`: under `bypass` `/ship` withholds the covered form, the classifier re-engages, and an unattended self-merge parks — no naked merge hole (Directive #584 Constraint 1). Dogfood-only (`.claude/settings.json`; cross-target injection deferred). (#587)
- New **local git-hook enforcement tier**: committed `.githooks/` adapters (pre-commit, pre-push, commit-msg) that reuse the existing helper checks (protected-branch, secret scan, commit-format) extend the working-flow to ANY local git op — human/terminal/script, not just the Claude Code agent. Activate per-clone with `scripts/install_git_hooks.sh` (repo-local `core.hooksPath`; `--check`/`--uninstall`); `/onboard` installs it and a SessionStart drift check nudges when it is inert. Advice tier (folds to `--no-verify`); server-side authority is a deferred follow-up. (#604)
- Added the tier-3 server-side enforcement authority (`scripts/install_branch_protection.sh`): a capability-adaptive escalation ladder that SETs default-branch protection where the caller is admin — a named per-repo ruleset `ghjig-tier3` (updated by id, zero-clobber, default-rename-safe) or, where rulesets are unsupported, a classic branch-protection floor-merge PUT (never a destructive replace) — verifies the five facets for any actor via `GET rules/branches ∪ GET branches/protection` (`configured`/`partial`/`absent`/`unreadable`, fail-closed honest), and degrades to report+prescribe without admin. `/onboard` now surfaces the tier-3 state and prescribes (never auto-mutates); the bypass surface (`bypass_actors`/`enforce_admins`) is reported, never forced. This completes SPEC §6.7's three-tier model — tiers 1–2 advise, tier 3 is the non-bypassable authority. (#613)

### Changed

- Tightened the write-capable `implementer`'s tree-safety: it now stages only the manifest-named paths (never `git add -A`/`-u`), and `/implement` surfaces a dirty working tree before dispatch (surface/warn, not a hard block). (#516)
- `/recall` now routes proactively: a trigger-oriented skill description plus a CLAUDE.md norm make the agent reach for it at recall-shaped moments — both user-asked ("have we decided X before?") and self-identified before planning/a decision — without restating the §5.25 contract or building the still-deferred forced auto-run. (#520)
- `/recall` now auto-escalates to the deep comment sweep when a light recall you ran for a user-asked question comes back empty — you no longer have to type `--deep` to reach a decision buried in an issue's comments. The self-identified pre-planning recall stays explicit-`--deep`-only. (#528)
- Plan review is now an adversarial contest: the planner produces one base plan, two independent `plan-challenger` agents attack it on distinct auto-selected axes (parallel + worktree-isolated), and `plan-reviewer` judges the three candidates — replacing the old model where the planner graded its own self-authored alternatives. (#530)
- Renamed the shell's internal identity to **GHJig-Claude** (the repo was already `ghjig-claude`): the entry binary is now `bin/ghjig` (invoke `ghjig`), every `CLAUDE_ENG_*` environment variable is now `GHJIG_*` (e.g. `GHJIG_SHELL_ROOT`), the in-command skip sentinel is now `# ghjig:skip=<cat>`, and the dogfood `settings.json` resolves its hooks via `${CLAUDE_PROJECT_DIR}` so the rename can never disarm a running session. Clean cut — the old `claude-eng` command and `CLAUDE_ENG_*` vars are gone and fail loudly rather than silently. (#533)
- Shortened the per-project binding-symlink name from `.claude/ghjig-shell-root` to `.claude/ghjig-root`; injected target repos must re-run `scripts/register.sh <path>` to repoint the binding (the env var `GHJIG_SHELL_ROOT` is unchanged). Also restored the released `CHANGELOG.md` 0.6.0 history that the #533 rename had inadvertently mangled. (#535)
- Retired the ambient `GHJIG_SHELL_ROOT` environment variable: hooks and scripts now self-locate the shell root via `BASH_SOURCE` and export `GHJIG_ROOT` internally, so the ambient env can never redirect which shell tree the hooks load (the cross-clone contamination vector). Migration: unset any global `GHJIG_SHELL_ROOT` export from your shell profile — it is now ignored (a once-per-session SessionStart banner names the leftover); test seams use `GHJIG_ROOT_OVERRIDE`. (#537)
- Completed the zero-env-input root resolution begun in #537: no shell code consults the ambient environment to find the shell root any more. The CLI scripts and lib consult sites self-locate from `BASH_SOURCE`, `.claude/commands/*.md` resolve the shell structurally through the per-project `.claude/ghjig-root` binding symlink (runtime-executed lines via a `git rev-parse --show-toplevel` prelude, passive template refs via the relative path) instead of `$GHJIG_ROOT/`, and the dogfood repo now carries a committed `.claude/ghjig-root -> ..` self-symlink so every registered project — targets and the shell repo alike — resolves by one uniform rule. A plain `claude` session (no ghjig launcher, no global env) now drives command flows correctly. (#539)
- CI shellcheck is now version-pinned (`0.11.0`, SHA256-verified fail-closed) and both CI and local runs go through `./scripts/lint.sh`, so "clean locally" predicts "clean in CI"; the per-file split is retained and a Linux peak-RSS flag guards the smoke.sh memory cliff. (#545)
- The `plan-reviewer` steelman is now **symmetric** across all contest candidates instead of steelmanning the incumbent (Plan A) alone. The former A-only step authored advocacy for A alone, autoregressively priming the judge toward the incumbent; it is split into an **invariant limb** (Check 0 — argue each candidate's strongest invariant-preservation case, retaining A's) and an **axis limb** (head of Check 1 — argue every candidate's axis-merit case with no fixed incumbent-first order, A still advocated as strongly as the challengers). This removes the generation-order privilege while keeping challenger axis-advocacy out of the invariant gate. (#573)
- The plan-challenger axis selection is now framed as a **weight, not a filter**: `/work-on` designates 2 *focus* axes out of the full menu (the rest stay in view via the domination bar) and the `plan-challenger` mandate reads "beat Plan A, leading with the assigned axis while holding all axes in view" — a framing alignment with the existing domination logic, not a change to it. `plan-reviewer` gains an **axis-selection sanity check** (an after-the-fact judge backstop confirming the focus axes fit the change, keeping the selector itself non-adversarial), and `/work-on` emits an observe-only **drop-axis attestation** (`audit_log info plan-axis`, P3 observe-before-harden) recording the focus/deferred split for a future friction consumer. (#575)
- The pre-merge review-integrity gate is now `merge-review` — it blocks `gh pr merge` unless a passing GitHub review is pinned to the current PR head (native APPROVED@head, or a self verdict=approve marker where author==PR-author==merger at commit_id==head), replacing the retired forgeable local `merge-attestation` file; a skipped/stale-head review or an outstanding CHANGES_REQUESTED blocks, it fails closed on any `gh`/lookup failure, and it is governed by the new `.claude/state/review-gate` toggle (`required` default / `bypass`, unknown→required, every `bypass` merge loudly audit-logged). (#586)
- The `permissions.allow` classifier exception for the exact `/ship` merge command is now injected into every onboarded target via `settings.injected.json` (previously dogfood-only), so a target gets the classifier exception paired with its already-injected `merge-review` gate and unattended `/ship` can reach merge instead of parking. (#591)

### Fixed

- `/recall --deep` now finds decisions recorded in comments even when the query is a multi-token natural-language phrase: the candidate search falls back to a per-token sweep when the whole-phrase search returns nothing, so the comment scan is no longer silently skipped. (#526)
- A pushed PR can no longer be merged on a stale or unreviewed head: reviewer subagents pin their artifact to the pushed PR head and the invoking skill blind-compares each reviewer's independently reported reviewed head against the true head (a mismatch or absent head is a fail-closed invalid vote, never counted toward the ship/review majority), and an un-skippable pre-merge hook now blocks `gh pr merge` when the local branch has unpushed commits (push-parity) or when the review was skipped (no attestation file, checked with no network) or performed at a stale head (merge-attestation). (#544)
- Hardened CI guardrails: the Issue→Project mirror no longer blanks the Parent field on a web-UI CRLF or trailing-text marker and degrades gracefully on transient API errors; check-changelog no longer conflates a `gh pr diff` transport failure with a missing fragment and now rejects a fragment whose bullet and `(#N)` ref are split across lines; the bare-`Refs` detector no longer silently drops a directive-scoped Ref past sorted position 5 (#553)
- `scripts/ac_closeout.sh` no longer deadlocks the merge gate and no longer re-pings @mentions: its idempotency guard now mirrors the `ac-closeout` gate exactly (skips only on the strict `## AC closeout (resolved by PR #N)` marker from a trusted-filer comment, so a loose or untrusted lookalike can no longer make the remedy skip while the gate keeps blocking), and it neutralizes `@mentions` in the acceptance-criterion lines it refiles into the closeout comment. (#556)
- Tier-2 dir-mode onboarding no longer half-installs its labels: two `initiative:*` label descriptions exceeded GitHub's 100-char cap, so `gh label create` returned 422 and aborted `ensure_v3_labels.sh` mid-run under `set -e`. Both descriptions are shortened to ≤100 chars and label creation is now fail-soft (one label's failure warns and continues instead of aborting the whole run). (#596)
- Unattended `/ship` can now complete an agent self-merge end-to-end: the head-pinned self-review it must post is issued via a fixed-form, wildcard-free `permissions.allow`-covered wrapper (`scripts/ghjig_file_review_post.sh`), so the auto-mode classifier defers it to the shell instead of blocking it as self-approval. Whether a self-review is honored is a new per-project policy — `.claude/state/self-review` (`allow`|`deny`, default `deny`, fail-closed) read by the `merge-review` gate — so the exception ships to every onboarded target but self-merge stays off until the operator opts in. (#598)
- Unattended `/ship` self-merge now posts its head-pinned self-review in-harness — the self-review producer reads the review body from an out-of-band state file instead of stdin, so the classifier-deferred wrapper stays byte-exact-invocable and no longer blocks as self-approval. (#602)
- The PreToolUse `--no-verify` matcher no longer false-blocks a non-git command whose heredoc/quoted body merely mentions the tokens (e.g. filing an issue *about* the flag) — the arm now strips heredoc data before scanning, matching its sibling arms. (#605)
- The PreToolUse `--amend` matcher no longer false-blocks a command whose heredoc/quoted body merely mentions the amend flag (e.g. committing or filing an issue *about* amending) — the arm now strips heredoc data before scanning, matching its sibling arms and the `--no-verify` fix in #605; a genuine amend of an already-pushed commit still blocks. (#607)
- On a GHES target the `/file-review` review-posting path and the `merge-review` gate reading the review now pin `gh api` to the repo's host (derived from `gh repo view`), instead of gh's default host (github.com) — so the review actually lands on the target host, the merge gate can read it, and the acting-identity lookup stops mis-computing self-merge. `/file-review` URL validation is now host-agnostic (accepts a pull URL on any host). (#610)
- On a GHES target, two non-review-path `gh api` sites left out of #610 now resolve the repo's host instead of gh's default (github.com): `/onboard`'s branch-protection check (was 404/mis-reporting the enterprise repo's protection) is host-pinned via `gh repo view --json url`, and `/release`'s changelog footer link derives the repo host (was hardcoded to `github.com`, and its origin-parse only matched github.com so a GHES release emitted no link at all). (#614)
- Unattended `/ship` no longer false-parks a clean PR when a CI check failed then passed on re-run: `ship_classify_blocker` now dedups `statusCheckRollup` to the latest check-run per check (by `completedAt`/`startedAt`) before its pending/failure test, so a superseded stale entry no longer masks the current green state (a genuinely-failing latest check still parks). (#615)

### Security

- Broadened the secret scanner to block GitHub App server-to-server (`ghs_`, including the Actions `GITHUB_TOKEN`), user-to-server (`ghu_`), and refresh (`ghr_`) token families, which previously committed clean; hardened the `scripts/ghjig_skip.sh` escape-token writer to reject a category outside `^[A-Za-z0-9,_-]+$` (closes a `../`-traversal on the token path). (#551)
- Closed seven guardrail matcher gaps (all preserving or tightening the block-path): the Edit/Write carve-outs now lexically normalize the target (a pure-string `.`/`..` collapse, so it holds even when the carve-out ancestor is absent) so a `..`-traversal can't borrow a carve-out to escape the out-of-scope gate; a bare bundled short force-push (`git push -uf`/`-fu`) now hits the force-push fail-safe; `mv -- <src> <out-of-registry>` no longer evades the destructive gate; a destructive operand literally named `env`/`time`/etc. is now scope-checked; the trusted-filer `--repo`/URL target rejects `.`/`..` path segments; `is_protected_branch` matches names case-insensitively; and `git reset --hard`/`git clean -f` named only inside a commit message no longer false-block. (#555)
- The `merge-review` hook now blocks a covered-form (`gh pr merge --auto --merge --delete-branch`) agent self-merge (PR author == merger, fail-closed on the author/merger lookup) even under `review-gate=bypass`, closing the disclosed-and-accepted residual (§5.7.1) where the static `permissions.allow` classifier exception was the sole guard for that exact form; non-covered shapes (the classifier re-engages) and human-initiated merges stay admitted under `bypass`. (#592)
- Made the unattended `/ship` classifier's per-check dedup tie-break fail-closed: when same-name check-runs lack timestamps, the compound `[timestamp, severity]` sort key now breaks the tie toward the more-severe state (FAILURE/CANCELLED/TIMED_OUT > PENDING/IN_PROGRESS/QUEUED > SUCCESS) instead of relying on array position, so a degenerate tie can never drop a FAILURE/PENDING in favor of a SUCCESS on the merge-decision path. (#617)
- Made the unattended `/ship` classifier (`ship_classify_blocker`) fail closed on a degenerate stdin payload: a malformed, non-object, or whitespace-only payload now defaults to `hard` (via a `jq -e 'type=="object"'` input guard) instead of falling through to `clean`, so a garbage payload can no longer route toward an unattended merge — matching the empty-stdin and jq-missing fail-closed guards. (#621)
- Hardened the tier-3 branch-protection tool's (`scripts/install_branch_protection.sh`) 404 discrimination: all four HTTP-404 checks (the two verify GETs, the path-1 rulesets-list gate, and the path-2 current-protection GET) now match the structured HTTP-404 status phrasing via a single-sourced `_gh_err_is_404` helper instead of a bare `404` substring, so a non-404 (403/5xx) error whose message incidentally contains "404" is no longer mistaken for a genuine 404 — the fail-closed gates stay closed and verify stays `unreadable` rather than false-`absent`. (#622)

## [0.6.0] — 2026-06-29

### Added

- Added an opt-in Code-phase **implementer** subagent and the `/implement <issue#>` command (Directive #477, EI-1): the implementer authors the Code phase from an explicit manifest (the Plan + the failing test + the named relevant files), iterates in its own ephemeral context, and returns only a structured result (commit/diff + plan-deviations + discoveries) — so within-Execution churn never enters the main loop, while the manifest is the selective-injection dual. It is **not** the default — nothing auto-routes the Code phase to it (a smoke lock guards this), pending the Directive's A/B measurement gate; invoke it explicitly via `/implement` (#486)
- Added the **directive-close** PreToolUse matcher (#490): a GitHub close keyword (`close`/`closes`/`closed`, `fix`/`fixes`/`fixed`, `resolve`/`resolves`/`resolved`, case-insensitive) followed by `#N` is now blocked when `#N` is a **Directive** Issue — in either the inline `--body`/`-b` of `gh pr create`/`gh pr edit` or a commit message (subject **or** body). At merge GitHub would auto-close the referenced Issue, which for a Directive bypasses `/complete-directive`'s signal-evidence gate (SPEC §5.13) — the live regression where PR #489's `closes #477 signal 3` force-closed Directive #477 while its signal 4 was still pending. Execution Issues are unaffected (`Closes #<execution>` is the normal resolving trailer) and a non-close mention (`Refs #N`, `advances #N`) always passes; the matcher fails open per `#N` on an unresolved type. The inline body is read; `--body-file`/`-F` is a documented residual (#490)

### Changed

- Closed the directive-level coding-memory loop for the opt-in implementer (Directive #477, EI-2): `/reflect` now distills a `### Learnings for the next Execution` section onto the parent Directive (durable codebase gotchas / conventions / reuse signal + the implementer's structured-return discoveries; within-Execution churn excluded), and `/work-on` reads the accumulated learnings back into the planner input and the `/implement` manifest's new `directive-level learnings` field — so the isolated implementer is fed directive-level knowledge instead of re-discovering it each Execution (the selective-injection half of the narrowing dual). Advisory context, never gating (#488)
- The isolated Code-phase **implementer** is now the **default** `/work-on` route (Directive #477 signal-4): `/work-on` dispatches `/implement` for Phase C by default, with a documented opt-out for trivial / one-line / glue edits and orchestrator glue (which stay in the main loop). The formal A/B measurement gate was descoped by maintainer decision — the context-narrowing hypothesis is adopted as operating policy on circumstantial evidence (the convergent mainstream agent pattern + the native subagent context-discard), hedged by fail-open reversibility (a missing implementer path degrades to main-loop authoring; the documented opt-out makes the default revertible) rather than a measured result. `implementer.md` / `/implement` / SPEC §1.2/§3.7/§4.12/§5.28/§1.9 and the smoke §126b/§126c lock are flipped from the opt-in posture to default-with-opt-out (#492)
- `/work-on` now resolves the repo's default branch (`gh repo view --json defaultBranchRef`) for the `Closes`/`Refs` trailer instead of assuming `main`, so on a `master`/`release`-default repo a PR to the trunk auto-closes its issue correctly (was: bare `Refs` → never closes). Also migrates the `block-directive` / `resolve-discussion` / `complete-directive` comment-posters to `--body-file` (the dir-mode injection-safe bar). EI-6 of the hook-enforcement hardening directive. (#504)

### Security

- The merge-strategy, ac-closeout, and trusted-filer-mutate PreToolUse gates no longer let a leading `gh` global flag (`gh --repo o/r pr merge`, `gh -R o/r issue close`) walk past their entry anchor — the anchors now tolerate a leading global-flag run while still requiring the subcommand adjacent (and not over-matching a `merge`/`close` word in a `pr create` body). EI-1 of the hook-enforcement hardening directive. (#499)
- The ac-closeout merge gate no longer accepts a forged `## AC closeout` comment (now requires the canonical `(resolved by PR #N)` shape from a trusted author), evaluates the correct PR when merging by URL (`…/pull/N`, including inside flag values), and detects unchecked AC written with any task-list bullet (`-`/`*`/`+`/`N.`); the `scripts/ac_closeout.sh` remedy tracks the same bullet family so there is no gate/remedy deadlock. EI-2 of the hook-enforcement hardening directive. (#500)
- The sensitive-file Edit/Write guard now matches case-insensitively (`.ENV`, `key.PEM`) and uses the documented `credentials*`/`id_rsa*`/`id_ed25519*` prefix globs (so `credentialsX`/`id_rsa_backup` are caught) plus `*.pem.*` double-extension backups — closing naming-based evasions of the key-material gate. EI-3 of the hook-enforcement hardening directive. (#501)
- SessionStart now surfaces a **zeroed scope-guard registry** (a present-but-empty `eng-state/registry.txt` silently disables all enforcement via the by-design empty-registry fail-open) with a once-per-session banner + a persistent `registry-zeroed` audit record — so the disarmed state is no longer traceless. The empty-registry fail-open itself is unchanged (no §1.4 flip); the binding-symlink-repoint disable is documented as an accepted residual (structurally undetectable from within). EI-4 of the hook-enforcement hardening directive. (#502)
- The protected-push and force-push guards no longer let an unlisted leading git global flag (e.g. `git --no-lazy-fetch push origin main`) drop the `git <verb>` match — `GIT_PREFIX` now tolerates a generic leading `--long[=v]`/`-X` flag run while preserving value-flag consumption (`-c <kv>`). The contrived `-c alias.<x>=<verb>` rename bypass is a documented SPEC §6.1 residual. EI-5 of the hook-enforcement hardening directive. (#503)
- Closed four narrower enforcement gaps: a flagless `mv`/`cp` clobbering an out-of-registry destination is now gated; the trusted-filer gate accepts the equals form `--reason=completed`; the line-1 `Parent Directive`/`Parent Initiative` markers tolerate a trailing CRLF; and `.shellsecretignore` now documents that its whitelisted paths (`*.md`, `docs/`, `tests/`, …) have no secret-scan coverage. `find -delete`/`truncate`/`>`-redirect clobbers remain documented SPEC §6.1 residuals. EI-7 of the hook-enforcement hardening directive. (#505)

## [0.5.0] — 2026-06-25

### Added

- Added `scripts/setup.sh`, a single host-side entry point that collapses first-run setup into one relative-path command: dependency check → register (local path) or clone (repo URL) → mechanical onboard pre-flight → an always-offered dir-mode install gate (default N) → next-step guidance (`--enter` launches the session directly). The PATH `export` is now optional — only needed to invoke `claude-eng` from an arbitrary directory (#459).
- The SessionStart banner now includes a once-per-session, zero-network SSOT-presence health line: `SSOT: MISSION.md ✓ SPEC.md ✓` when both are present, or a prominent SPEC-first nudge when `SPEC.md` is absent (SPEC is the required behavioural SSOT, SPEC §1.3). It consumes the shared `scripts/lib/onboard_checks.sh --dry-run` and surfaces only for a registered target (#461).
- Added `scripts/spec_drift_candidates.sh`, a measure-first advisory reader (sibling of the `*_candidates.sh` family) that surfaces code-ahead code-vs-SPEC drift candidates — commits that touched a path `SPEC.md` references without co-updating `SPEC.md` — in the `  <path> | drift-commits=N` cluster shape. Detection only; configurable via `SPEC_DRIFT_LOOKBACK` / `SPEC_DRIFT_MIN_COUNT` (#463).
- Added the `/reconcile-spec` command: it consumes `scripts/spec_drift_candidates.sh`, classifies each code-vs-SPEC drift candidate into one of three dispositions (spec-ahead → track; code-ahead-correct → propose a SPEC update, applied only after explicit user approval; code-wrong → never edit SPEC, route to a code fix), and audit-logs each decision under `spec-reconcile`. It never silently rewrites SPEC to match possibly-buggy code, and never auto-applies a correction in unattended mode (#465).
- `/release` now runs a stack-aware, verification-only manifest-version preflight: it resolves the detected project's native version field (node `package.json`, python `pyproject.toml`, rust `Cargo.toml`; other stacks skip) via the single-source `detect_version` and refuses a release whose manifest version contradicts the `X.Y.Z` being cut (naming both + the bump fix). An unknown stack / absent or unparseable field degrades to a graceful skip; the manifest is read, never written (#470).
- Added a file-based **in-agent escape channel** (`scripts/eng_skip.sh` writer + a `should_skip` token reader): a one-shot, command-fingerprint-bound, 60s-TTL skip token under the per-project state dir that the PreToolUse hook reads at fire time — restoring a working escape inside the Claude Code Bash tool, which strips the in-command `SKIP_HOOKS` forms (#478). Fail-safe-to-block on any doubt, consumed on read, category-scoped, and audit-logged (parity with `SKIP_HOOKS`). `/release` and `/bootstrap-repo` now arm a token via `eng_skip.sh` immediately before their protected-branch commit; the in-command forms remain valid where a command arrives verbatim (a real shell, the smoke harness). The shell cannot change the harness's command preprocessing, so the channel lives outside the command string by design (#479)

### Changed

- Re-centered MISSION "The mechanism" on the harness-as-rising-floor model and added a SPEC §1.9 harness-overlap classification — every shell mechanism marked `cede-to-harness` / `keep-as-policy` / `keep-as-safety-redundancy` — guarded by a smoke coverage-parity assertion. (#451)
- SPEC.md is now required for any repo that carries code (the behavioural-SSOT framing of SPEC §1.3), not only once an external CLI/API/schema/protocol contract exists; the mechanical `/onboard` checks (upstream, permission, SSOT presence, branch protection, CI) are extracted into a single shared `scripts/lib/onboard_checks.sh` that `/onboard` and the forthcoming `setup.sh` both consume (#457).
- The SessionStart friction advisory now also runs `scripts/spec_drift_candidates.sh` and reports the outstanding code-vs-SPEC drift-candidate count (`N spec-drift candidate(s)`) once per session, under the same timeout envelope + once-per-session stamp as the other readers — so SPEC drift surfaces without a human running the script (#467).

### Fixed

- `/reflect` enrich-in-place PATCH now uses `gh api -F body=@-` (reads stdin) instead of `-f` (which set the literal string `@-`), so enriching a Directive reflection comment no longer corrupts its body; guarded by a smoke check against the broken form in any command doc. (#452)
- `/release`'s documented post-merge `gh release create` one-liner no longer passes `--verify-tag`, which always aborted on a fresh release (the tag is created together with the Release, so the flag's pre-existing-tag requirement could never be met). It is also followed by a new read-only, fail-open `scripts/release_verify.sh <X.Y.Z>` that confirms the `vX.Y.Z` tag + a GitHub Release with non-empty notes actually landed, surfacing an advisory on a missing tag / missing-or-empty-notes Release; resolves #448 (#472).
- Fixed `release_verify.sh` falsely reporting "empty notes" when `jq` is absent or fails (it now fails open with a named advisory), and hardened the post-merge release-integrity safety net: re-anchored two non-discriminating smoke assertions, added a self-policing guard for the SPEC §8 `scripts/` tree, and closed the TOML manifest-version coverage gap (#473)
- Fixed the single-`&` (background operator) sibling of the #446 glued-separator force-push false-negative: a command gluing a background `&` onto a protected force-push (`git status&git push --force origin main`, no space) was folded by the env-prefix shlex round-trip and slipped past the gate. A lone unquoted `&` is now re-separated like the other separators — except when it is part of a redirect (`>&`, `&>`, `N>&M`, `<&`, `&>>`), which is left intact so legitimate commands (`git push --force origin feature 2>&1`) are not over-blocked. `push_segments` gains the same `&` split so a backgrounded sibling clause's token cannot collapse into the push segment (#476)
- Corrected the escape-hatch documentation (SPEC §7 + §5.0, CLAUDE.md, and the `escape.sh` helper header): in the live Claude Code Bash tool BOTH `SKIP_HOOKS` escape forms (leading env-prefix and trailing sentinel) are stripped before the PreToolUse hook reads the command, so neither takes effect in-harness — the prior docs incorrectly claimed the trailing sentinel survives in-harness. Recourse for a sanctioned guarded op is now documented: run it in a real terminal (no PreToolUse hook fires there) or via a non-protected branch + rename; a smoke guard prevents the false claim from regressing (#478)
- Fixed the #479 in-agent escape token channel being non-functional in the live harness: the writer (`eng_skip.sh`, a Bash-tool subprocess with `CLAUDE_PROJECT_DIR` unset) and the reader (the PreToolUse hook, with it set) resolved different `eng_state_dir` directories, so the armed token was never found. The writer now derives `CLAUDE_PROJECT_DIR` from the git top-level when unset (matching what the hook sees), and both empty-`eng_state_dir` fallbacks are realigned to `.claude/eng-state` — so writer and reader always agree. A new smoke arm exercises the resolution without the `ENG_STATE_DIR_OVERRIDE` seam that masked the divergence (#483)

### Security

- Fixed a false-negative on the force-push/protected-branch PreToolUse gate: a command that glued a separator (`&&`/`||`/`;`/`|`) onto a following push with no surrounding space was folded by the env-prefix shlex round-trip, so the trailing protected force-push slipped past unblocked. Glued separators are now re-separated (quote-aware, leaving in-message literals untouched) before matching, so glued and spaced forms gate identically (#446)

## [0.4.0] — 2026-06-23

### Added

- `/recall <topic>` — episodic retrieval over the project's own decision record (closed issues / merged PRs / ADRs), returning pointers only (number + one-line title, never a body), bounded by `RECALL_LIMIT` (default 5 per substrate) and fail-open when `gh` is unavailable (#429).
- `scripts/test/mutation.sh` — an enforcement-matcher mutation harness (new `mutation` CI job) that measures the smoke suite's kill-rate on the critical matchers (commit-format / secret / protected-branch), failing if a silently-weakened guard survives the suite (#430).
- `/replan-check` — an in-loop checkpoint that compares the working diff against the Plan and re-invokes `planner` only on *structural* divergence (out-of-plan load-bearing files or an unreachable acceptance criterion), keeping the Plan a faithful task-scoped manifest. Advisory, not a gate; referenced post-sync by `/sync-pr` (#434).
- High-asymmetry reviewer tier (SPEC §4.11) — for decisions where a wrong approve is irreversible (security-surface merge to the default branch, force-push, Directive completion, ADR-at-merge), `/ship` and `/complete-directive` replace the single reviewer with an N=3 independent majority vote (`is_high_asymmetry` in `helpers/blast_radius.sh`); blast-radius-gated so the common path stays single-reviewer, fail-open to single reviewer on a helper miss (#435).
- `eng_commit <type> <issue> <subject> [body…]` (`helpers/eng_commit.sh`) — a slot-assembly helper for the commit subject: validates `<type>(#<issue>): <subject>` against the conventional-commit rules *before* committing and builds the message as a bash argv array, so multibyte/multi-paragraph bodies round-trip cleanly and the commit-format hook sees and accepts the subject. Offered (not forced) by `/work-on`; the hook stays the net (#438).

### Fixed

- `/release` now prescribes the scopeless `chore: release X.Y.Z` commit subject so the release commit passes the conventional-commit hook unmodified — the old `chore(release):` form was rejected by the `commit-format` matcher and not covered by the `SKIP_HOOKS=branch` escape (#418).
- `scripts/test/smoke.sh` no longer prints a spurious `No such file or directory` stderr line on a clean run — the §357 live-sink snapshots now guard absent legacy paths with `[ -f ]` before the input redirect, leaving the assertion semantics unchanged (#419).
- The force-push matcher is now segment-isolated (like the protected-push arm, #366): a force flag, protected token, or push target named in a *sibling* non-push segment (a force-bearing push composed with a PR-create on the default branch) or a heredoc body no longer false-trips the gate, and a bare force-push composed with a sibling is no longer wrongly allowed. Every genuine-block path (protected / bare / `--mirror` / case-fold / multi-segment) is preserved (#439).
- The force-push / protected-branch matchers no longer false-trip on a git literal documented inside a commit *message* (`-m` / `--message` / `-F` value) — a new `strip_command_data` `message` mode elides the message-flag values before scanning, so the shell (and adopters) can document git patterns in a commit body. The elision is anchored to the message flag, so a genuine quoted push target and a real sibling push segment are preserved, and an unclosed message quote fails closed (#443).

## [0.3.0] — 2026-06-19

### Added

- New `/bootstrap-repo` stage-0 skill takes a brand-new target with no default branch (empty repo / unborn HEAD) to a seeded default branch (`MISSION.md` + `README.md`) ready for `/onboard`, owning the scoped, audit-logged first-commit exception for the seed commit (SPEC §5.0). (#307)
- Per-project binding: registered targets now resolve the shell via an untracked `.claude/eng-shell-root` symlink + `$CLAUDE_PROJECT_DIR` (with hooks self-locating their root), so a plain `claude` enforces with **no global `CLAUDE_ENG_SHELL_ROOT` env / no `~/.zshrc` export**. The shell's own `settings.json` stays env-based (dogfood unchanged). (#312)
- Per-project ephemeral state: the audit log and `.claude/state` caches now resolve via `eng_state_dir()` to `$CLAUDE_PROJECT_DIR/.claude/eng-state/` in hook context (else the legacy shared path), so two projects on the same shared codebase have mutually-invisible audit/caches. The scope-guard registry move is a separate follow-up. (#314)
- Per-project isolation is now surfaced to users: the statusline shows the bound canonical shell root and whether ephemeral state is `project-local` vs `legacy-shared`, and `claude-eng list` prints the registered target projects (advisory — workspace symlinks ∪ legacy registry). The obsolete `inject-consistency` SessionStart banner was removed (its env-unset premise died with the #312 binding; the residual no-op is undetectable from SessionStart). (#318)
- The shell repo now carries its own `.github/PULL_REQUEST_TEMPLATE.md` (derived from `pr_template_for_target.md`, kept in sync by smoke), so manual/web-UI PRs against the shell open with the same Doc/Test/Code + Ship-gate scaffold it ships to targets. (#320)
- New `resolve_work_lang` resolver (`.claude/hooks/helpers/work_lang.sh`) + SPEC §5.7.2 establish the work-language foundation: durable repo artifacts resolve a configurable work language (`CLAUDE_ENG_WORK_LANG` → `.claude/state/work-lang` → default `en`, any code accepted) separate from the conversation language. Statusline surfacing and skill integration follow in later EIs of #322. (#323)
- The status block now surfaces the active work language: `/status` and the per-turn statusline show a `work-lang:` line (and `status_json` a `work_lang` key), resolved via `resolve_work_lang`, so the language of durable artifacts is visible alongside `mode`. (#325)
- The artifact-authoring skills (`/file-issue`, `/file-directive`, `/work-on`, `/ship`, `/complete-directive`) now each carry a `## Work language` note instructing the agent to recast context and author their durable artifact in the configured work language (`resolve_work_lang`, SPEC §5.7.2) — completing the communication-vs-work-language separation of Directive #322. (#327)
- `claude-eng <dir>` now runs a launch-time binding-health check: if an injected target's `.claude/eng-shell-root` binding is missing or dangling, it warns (non-blocking) before starting the session and names the repair (`scripts/register.sh`). This surfaces the broken-binding silent-no-op that the #318-removed SessionStart banner structurally could not detect (the hook runs through the broken link). (#334)
- New `/changelog` skill authors a changelog fragment (or applies `skip-changelog`) in-flow, and `/ship` now gates the ready transition on the fragment-or-skip contract so it is satisfied before CI rather than after a red check. (#346)
- SPEC is now a first-class target SSOT paired with MISSION: a lightweight `spec.md` template, a path-parameterized `build_toc.sh` (`--spec`), and a tier-3 `check-toc.yml` ToC-freshness gate shipped to targets. (#347)
- Named the positive/negative enforcement principle: a new MISSION "The mechanism" action-axis paragraph plus SPEC §6.0 "Enforcement style" (P1 cost-asymmetry, P2 enforcement physics, P3 maturity migration, P4 pairing) give a canonical standard for evaluating hook and guidance proposals. (#353)
- New tier-3 substrate workflow `initiative-feedback-label.yml` projects `/initiative-feedback`'s comment markers into labels for the orchestrator: an `issue_comment` whose body begins with `## Initiative challenge` gets `initiative:challenged`, and one beginning with `## Initiative completion` gets `initiative:completion-requested` (an unrelated comment gets neither). The two labels are added to `scripts/ensure_v3_labels.sh` and the workflow installs via the existing tier-3 glob. The label is applied by the target-repo CI Action, never by eng's runtime, preserving the `initiative-readonly` boundary. (#359)
- Audit records now carry an additive `source` field (`live` for real sessions, `test` for harness fixture fires, set only via the harness-owned `CLAUDE_ENG_AUDIT_SOURCE=test` marker a real action cannot inject — fail-safe-to-`live`), and the three reviewer gates (`issue-reviewer`/`plan-reviewer`/`activation-reviewer`) emit a categorized reject record (`reviewer_reject_audit` → `warn <issue-review|plan-review|activation> reject "class=<reason-class> …"`) on a non-pass verdict. Together these make the §6.0 P3 enforcement migration observable from the audit log (Directive #356 signals 1+3). (#361)
- Audit-log consumers for the §6.0 P3 advisory→hook→narrowed migration (Directive #356 signals 2/4/5): `scripts/narrowing_candidates.sh` surfaces escape-clustering over LIVE records (same category/reason across distinct days) as gates that may warrant narrowing, and `scripts/promotion_candidates.sh` aggregates the reviewer-reject reason-class trail (#361) into advisory→hook promotion candidates above a threshold. Both are read-only and surfacing-only — they list candidates; the narrow/promote decision stays human/reviewer judgment. SPEC §6.0 P3 + MISSION "The mechanism" now state the dual-positive-channel concept (a block's in-band message is its immediate positive face; the audit line is a deferred positive face, latent until a consumer is wired). (#363)
- Stop-hook nudge: when commits land after the last `/sync-pr`, the shell now suggests `/sync-pr` so the PR-body checklist stays in sync with HEAD (SPEC §6.3 second arm). (#375)
- New `/flush` skill distills task-scoped session state into durable memory (PR body + `.claude/state/` record) before a context clear, so clearing is `active → archived` not `active → delete` — the flush half of the new flush → clear → reconstruct lifecycle (SPEC §3.7). (#387)
- PostToolUse now nudges toward a targeted `Read --offset/--limit` (or `explorer` delegation) when a whole file is read above a line threshold — a positive, non-blocking narrowing affordance; see the new SPEC §1.8 inventory of in-session narrowing levers. (#389)
- Closed the friction-observability loop: a once-per-session, non-blocking, TTL-gated SessionStart advisory (SPEC §6.5(d)) now surfaces accumulated friction (escape/promotion clusters + a new additive `unattended-park` audit record bridging overnight park frequency into `audit.jsonl`) as a one-line pointer to `/audit` — so the shell sees its own friction without a human running a script (#398).
- New `scripts/ceremony_candidates.sh` measure-first consumer surfaces ceremony mis-sizing (a `feat` landing with no test/doc phase commit, or full phasing over a one-file change) through the SessionStart §6.5(d) friction advisory. (#402)

### Changed

- Per-project scope-guard registry: the registry now resolves via `eng_registry_file` to `$CLAUDE_PROJECT_DIR/.claude/eng-state/registry.txt` in hook context (and the target's own `eng-state/` on registration), so two projects on the same shared codebase have mutually-invisible registries. The legacy shared `.claude/state/registry.txt` remains a read-only back-compat floor; fail-open posture and the shell-root carve-out are preserved. (#316)
- The `issue-reviewer`, `plan-reviewer`, and `code-reviewer` now evaluate enforcement-adding proposals against SPEC §6.0 (P1 cost-asymmetry face-match, P4 block-names-its-alternative / gate-behind-guidance), making the enforcement-style principle operative instead of inert prose. (#355)
- Re-synced SPEC §8 "Directory structure" and §16 build-order inventory to the actual tree — the block was ~3x stale (agents 6→9, commands 9→28, helpers 11→19, docs 4→6) and is now drift-guarded by smoke §103; §16 helper/command lists now point to §8's canonical enumeration instead of carrying a second copy (#392).
- SPEC contract-hygiene pass: documented why `helpers/ship_mode.sh` is exempt from the §6.1 `safe_source` fail-policy table, added the missing §9.6/§9.7/§9.8 body-specs for `directive.md`/`spec.md`/`readme_for_target.md`, moved the dangling `/wrap`+`SessionEnd` reference to the §0.4 deferred-capabilities list, and corrected §5.7 step-count wording + §5.21/§5.22 stale "milestone v2" heading provenance (#393).
- Budgeted the always-on injected tier: `.claude/CLAUDE.md`'s "What hooks enforce" section was rewritten from ~14 dense paragraphs restating SPEC §6.1 into a one-line-per-matcher pointer index (17767→10281 bytes), the §9 thin-pointer rule now binds CLAUDE.md with a `wc -c ≤ 12000` budget drift-guarded by smoke §104, and SPEC §6.5(b) was reconciled to the actual native-load behavior (#396).
- README now works as an adoption runbook, not just a landing page: a new "Adopting it on your repo" section documents the injection Footprint (what symlinks/state land in a target and that they stay git-excluded), the `claude-eng`-vs-plain-`claude` invocation paths, the `gh auth` + dir-mode `project`/workflows token scopes, a `/onboard` preview, and that dir-mode adoption mutates the target via a PR; the stale "547 assertions" figure is corrected and floor-guarded against the live smoke count. (#409)
- README Quick start reconciled with the "Adopting it on your repo" section (#409): a one-line prerequisites pointer now leads, `register.sh <existing repo>` is the primary path (clone-into-`workspace/` shown as a secondary one-liner), and the duplicated register/invocation detail is stated once in the Adopting section instead of twice. (#411)
- README now advertises `unattended` mode as the headline capability: the opening pitch names it (file→branch→Doc/Test/Code→review→merge with no human in the loop), and a dedicated "Attended vs unattended" subsection frames the overnight use honestly — reviewer subagents substitute for approval, hard blockers park with an audit trail, humans can override, direction is never autonomous. (#413)

### Fixed

- `check-changelog.yml` now fails a PR if **any** added changelog fragment is malformed, not only when *every* fragment is — a single valid fragment no longer masks a malformed sibling. The gate also now enforces the leading `- ` bullet form, and both the gate and `release_consolidate.sh` validate the same fragment contract so the per-PR check can't diverge from the release-time validator (SPEC §18.6). (#303)
- `check-changelog.yml` no longer startup-fails: a bash heredoc whose `EOF` terminator sat at column 0 dedented out of the `run: |` YAML block scalar and made GitHub fail to load the whole workflow, so the per-PR changelog fragment gate had silently never run. Replaced with an indented here-string; a new smoke §81 parses every workflow YAML to catch regressions. (#309)
- Directive reflections are no longer permanent boilerplate stubs: `/reflect` now classifies comments by content marker (`reflect-stub` / `reflect-enriched`) instead of the shared PR URL, so a Claude-in-loop run **enriches the workflow's at-merge stub in place** (PATCH by comment id) with the per-signal Contribution block — previously the URL-keyed idempotency made `/reflect` no-op against the stub forever. (#329)
- The dir-mode post-merge reflection resolver is now label-aware: it climbs the `Parent Directive: #N` chain to the first `directive`-labelled ancestor (depth-cap 2, cycle guard, fail-soft) instead of posting to the first marker it finds, so nested-umbrella work no longer mis-targets the umbrella issue. The resolution logic is extracted into a sourceable, smoke-tested helper (`resolve_parent_directive.sh`) shipped alongside the workflow, and a resolved-nothing run now logs a visible no-op. (#335)
- The dir-mode post-merge workflow now flags the SPEC §10.5 bare-`Refs`-to-default anti-pattern: when a PR merges to the live default branch with an empty closing set but a directive-scoped `Refs #N` in its body, it posts a single idempotent warning comment on the PR naming the lost reflection and the recovery. Warn-only, fail-soft, and — being merge-triggered rather than a `gh pr merge` hook — it catches human GitHub-UI merges too (the dominant #92 failure path). (#337)
- The `ac-closeout` and `merge-strategy` hook gates no longer false-trigger when `gh pr merge` appears only as command *data* — a heredoc body, a quoted argument, or a commit message — rather than as an actual command. Detection now strips heredoc bodies and quoted string literals before testing for the command words (shared `is_pr_merge_command` predicate), and stays fail-closed (any parse uncertainty, missing `python3`, or missing helper → still treated as a merge, so real merges are always gated). (#340)
- `setup_project.sh` now names the dir-mode Project discriminator field `Item Type` instead of `Type`: GitHub reserves `Type` as a built-in Projects-v2 field name, so `gh project field-create --name Type` failed with "Name cannot have a reserved value" and broke tier-3 onboarding at Project setup. The issues→project mirror workflow (both byte-identical copies) and SPEC §1.7 are updated in lockstep. (#342)
- `onboard_target.sh` tier-3 install now detects an **untracked** `.github/` and opens the substrate PR instead of silently skipping it. The change-detection gate used `git diff --quiet -- .github/`, which is blind to untracked paths, so a greenfield target (no pre-existing `.github/`) got "already match (no PR needed)" and never installed the dir-mode workflows. Switched to `git status --porcelain -- .github/`. (#343)
- `scripts/test/smoke.sh` now runs every fixture hook fire against an isolated ephemeral state dir (`ENG_STATE_DIR_OVERRIDE=$SMOKE_STATE`, exported for the whole run), so test runs no longer pollute the shell's live shared audit log (`.claude/audit/audit.jsonl`) or scope registry (`.claude/state/registry.txt`). Previously ~97% of the live audit log was smoke-fixture noise, making the friction/escape-clustering signal un-queryable. The resolver-contract tests (§83/§84) and the §20 audit-path test locally `unset` the override to keep exercising the other resolution branches; the dir-mode guard tests (§41/§50) now register their target on its own per-project `eng-state/registry.txt`. A standing in-suite assertion fails loud if a future change reintroduces a live-sink write. Restores the MISSION "shared code, per-project state" isolation invariant for the test path. (#357)
- SPEC's audit-record-shape examples now match what `audit_log` actually writes: the §6.1 helper-missing example and the §7 escape example carry the `source` field (added on every record by #361), and the `info` event kind (emitted by `audit_log info` flows) is documented in a new event-kind set so it no longer contradicts the "no new event-type kinds" rule. Doc-accuracy only; no behavior change. (#365)
- The protected-branch push gate no longer false-blocks when a protected token (`main`/`master`/`release/*`) appears only inside a heredoc body or in a sibling non-push segment of a compound command (e.g. `git push origin <feat> && gh pr create --base main`). Detection now heredoc-strips the command (shared `strip_command_data` helper, factored from #340) and scans only the `git push` command segment(s); the same heredoc-strip is applied to the `git clean -f` gate. Quoted literals are deliberately not stripped on the push arm, so a genuine quoted target (`git push origin "main"`) still blocks. Fail-closed: genuine protected pushes (bare/`-u`/`HEAD:main`/env-prefixed/non-first-`&&`-segment/quoted-target) still block, and any strip failure falls back to the full-command match. (#366)
- The commit-subject extractor (`extract_commit_subject`) no longer mis-reads the subject for three standard `git commit` shapes: a single `-m` with embedded newlines (now takes the first line, not the whole subject+body blob), multiple `-m` flags (now the first value, not the greedy last = body), and a commit chained after an unrelated `cat <<EOF` heredoc (the sibling heredoc is no longer grabbed as the subject). Extraction is bounded to the `git commit` segment and runs on the raw command; `-F`-only commits extract empty and fail open. SPEC §6.1.1 documents the contract. (#367)
- SPEC↔code accuracy sweep (surfaced by the SPEC gap analysis): dropped the stale `/onboard-dir-mode (deferred)` marker (the skill shipped); added the §3.3 scope-guard three-way fail-open contract to the SPEC SSOT (previously only in CLAUDE.md); widened the §6.1 `git clean` row to the force-flag cluster and the sensitive-file row to `id_rsa*`/`id_ed25519*` (parity with §14 + the code); documented the force-push `origin HEAD`-vs-bare "verified target" definition; forward-pointered the reviewer reject-audit (§5.2/§5.3/§5.12/§5.13 → §6.1) and the initiative-feedback label projection (§5.22 → §1.7); corrected the §2.1 dir-mode command range to §5.10–§5.22; and updated the §1.7 reversibility-preflight marker to reflect that the per-command substrate preflight landed (Directive #107 closed). Doc-only; no behavior change. (#368)
- `/onboard-dir-mode` tier-3 now installs the `changelog_unreleased/` authoring substrate (TEMPLATE.md + the six Keep-a-Changelog category dirs) into the adopter's repo root, not just the `check-changelog.yml` gate — closing the gate-without-substrate gap so onboarded repos get the fragment-gate and the affordance it enforces against (SPEC §18.1/§18.6). (#374)
- `bypass-suspect` hook no longer over-warns on benign data heredocs (e.g. `cat`/`wc` reading a heredoc, including a `.sh`/`.zsh` filename used as an operand); the warn is now scoped to shell-spawning heredocs as SPEC §6.1 specifies. (#375)
- The commit-subject extractor no longer mis-reads a normal `-m` commit as a heredoc message when its body merely mentions a `<<TAG` token: a single `-m` whose first line is a valid Conventional Commit subject is honored as the subject (line-1 precedence), so commits whose body discusses shell/heredocs are no longer wrongly blocked. The genuine `$(cat <<TAG … TAG)` form and fail-safe blocking of malformed subjects are preserved. SPEC §6.1.1. (#383)
- The protected-branch commit guard no longer false-blocks a `gh issue edit`/`gh issue comment` (or any command) whose quoted `--body`/heredoc text merely mentions a git commit invocation; only a real commit on a protected branch is blocked. (#403)
- Filer-trust resolution (`is_trusted_filer`) now reads `author_association` via `gh api`, fixing silent always-fail on gh versions where `gh issue view --json authorAssociation` is rejected as an unknown field. (#404)

## [0.2.0] — 2026-06-01

### Added

- `/release X.Y.Z` skill consolidates per-PR `changelog_unreleased/` fragments into a versioned `CHANGELOG.md` section, opens a `release/X.Y.Z` draft PR, and documents the post-merge tag + GitHub Release one-liner. (#131)
- `.github/workflows/check-changelog.yml` per-PR fragment-gate workflow; `skip-changelog` label opts a PR out. Distributed to adopting repos via `/onboard-dir-mode --tier 3`. (#133)
- New `/activate [<issue-#>]` skill gates every Issue through a `status:proposed → Active` review (single or no-arg batch); `activation-reviewer` now emits `pass`/`revise`/`reject` with structured refile fields, `/file-issue` stamps `status:proposed` (full symmetry), and a new `awaiting-author` label marks reviewer handoff. `/activate-directive` becomes a one-cycle alias. (#172)
- New `auto-clear-awaiting-author.yml` GitHub Action removes the `awaiting-author` label on `issues.edited`, so the reviewer→author handoff queue stays accurate after an author edits a `revise`/`reject`'d Issue (human-facing hygiene only — does not affect the `/activate` programmatic skip). (#180)
- Completed the dir-mode type taxonomy + filing gate: added a first-class `execution` label (Execution Issues are no longer identified by label-absence; the `Parent Directive:` marker is purely the parent pointer), aligned every work-type Issue template to apply its type label + `status:proposed`, and disabled blank issues (`blank_issues_enabled: false`) so every filing channel is typed and activation-gated. `task`/`execution`/`bug` stay distinct. (#186)
- Added a Korean translation of the README as `README.ko.md`, with a reciprocal language-switcher line at the top of both files; `README.md` stays the canonical source and `README.ko.md` tracks it. (#195)
- Added the `label-parent-consistency` runtime hook matcher: `gh issue edit <N> --add-label {execution|task|bug}` is now blocked when the label contradicts the Issue body's line-1 `Parent Directive: #N` marker — `execution` without a marker, or `task`/`bug` with one. This converts #197's advisory reviewer/skill prose into enforcement that fires regardless of which agent applies the label (hooks-as-environment). Scoped to the `--add-label` edit path (the `gh issue create` path is already consistent via `/file-issue`); fail-open when the Issue body can't be resolved. Escape: `SKIP_HOOKS=label-parent-consistency SKIP_REASON='<why>'`. (#199)
- Documented the **Initiative** tier (M0 foundation, milestone *v2 consuming initiatives*): a planning-tier strategic commitment one tier above a Directive, consumed — never authored — by the shell. MISSION gains a `## Consuming Initiatives` section + a "Not an author of Initiatives" non-goal; SPEC §1.7 generalizes a Directive's parent (a MISSION section **or** an Initiative), adds the load-bearing contract (an Initiative must carry a termination condition evaluable without knowledge of the code), and documents upward-only feedback (comment / challenge / completion), same-repo location, and scope. Model only — the enforcing mechanisms (the `initiative` label, `is_initiative_issue`, the three hooks, reviewer branching, the consume/feedback flow) are delivered by later milestone-v2 Directives (M1–M4) and are explicitly marked not-yet-enforced. (#246)
- Added the **recognition layer** for the Initiative tier (M1.1, milestone *v2 consuming initiatives*): onboarding now installs the `initiative` label (the planning-tier type-key, symmetric to and mutually exclusive with `directive`), and `helpers/issue_type.sh` gains an `is_initiative_issue` predicate (cached, fail-open) plus an `issue_has_initiative_parent_marker` tri-state resolver for the line-1 `Parent Initiative: #N` marker. This makes the shell *recognize* an Initiative and an Initiative-parented Directive; the enforcement matchers, reviewer branching, and consume/feedback flow remain forthcoming (M1.2–M4). The tier-2 label set is now 13 (11 from `ensure_v3_labels.sh` + inline `directive`/`initiative`). (#249)
- Added the **enforcement layer** for the Initiative tier (M1.2, milestone *v2 consuming initiatives*): three `pre_tool_use.sh` invariants make the tier coherent. (1) **Label mutual-exclusivity** — `gh issue edit <N> --add-label initiative` on a `directive` Issue (and `--add-label directive` on an `initiative` Issue) is blocked. (2) **Directive parent-XOR** — `--add-label directive` is blocked unless the body carries exactly one parent kind: a `## MISSION fit` field XOR a line-1 `Parent Initiative: #N` marker. Both fold into the existing `label-parent-consistency` matcher (`SKIP_HOOKS=label-parent-consistency`). (3) **`initiative-readonly`** (new matcher / escape category) — a mutating `gh issue edit`/`close`/`reopen` on an `initiative` Issue is blocked; `gh issue comment` is always allowed (the shell consumes Initiatives, never mutates them). All three fail open on unresolvable predicates. Adds the `issue_has_mission_fit_field` tri-state resolver. (#251)
- Added the **reviewer layer** for the Initiative tier (M2, milestone *v2 consuming initiatives*): the type-neutral `activation-reviewer` now recognizes the `initiative` label, branches a Directive's alignment check on its parent kind (a `## MISSION fit` MISSION section vs. a line-1 `Parent Initiative: #N` marker — judged against the parent Initiative's termination condition), and gains two checks the M3 consume flow invokes — **contract-evaluability** (an Initiative's termination condition must be evaluable without code knowledge, else it is under-specified and surfaced upstream) and **extraction-faithfulness** (Directives extracted from an Initiative must decompose its termination condition without smuggling strategy). Extended in place — no new subagent — to avoid the §4.9.3 session-restart caveat. SPEC §4.9/§1.7 updated. (#254)
- Added `/consume-initiative <N>` (M3, milestone *v2 consuming initiatives*): the consume flow that turns an upstream `initiative`-labelled Issue into Initiative-parented Directive proposals, working backward from its termination condition. A prompt-driven skill — type/substrate check → contract gate (`activation-reviewer` I1; an under-specified termination condition surfaces a plain upstream comment and stops) → working-backward extraction (each proposal carries a line-1 `Parent Initiative: #N` marker, no `## MISSION fit` field) → faithfulness gate (`activation-reviewer` I2 over the whole set) → file as `status:proposed` Directives (never activated; activation stays human/reviewer-gated). Read-only on the Initiative except the one under-specification comment; bodies filed via `--body-file`. Audit category `initiative-consume`. SPEC §5.21 / §1.7. (#257)
- Added `/initiative-feedback <N> {--challenge "<reason>" | --completion}` (M4, milestone *v2 consuming initiatives*) — the upward-feedback flow that completes the Initiative tier. **Challenge** posts a scannable `## Initiative challenge` comment carrying the code-derived contradiction (a real dependency/measurability/cost the planning layer cannot see) and requests re-evaluation — it escalates, never rejecting/retiring/closing. **Completion** posts a `## Initiative completion` comment carrying the merged-Directive evidence and requests termination assessment — it never asserts the condition is met or closes the Initiative. Comment-only (`initiative-readonly` backstop); `--body-file` for the injection surface; audit category `initiative-feedback`. With this, the milestone-v2 model is fully live: recognition + enforcement + reviewer + consume + feedback. SPEC §5.22 / §1.7. (#260)
- Added a **doc-consistency smoke discipline** (EI-C of doc-integrity Directive #264, smoke §74) that greps SPEC's hand-maintained enumerables against their actual sources — the agent count vs `.claude/agents/*.md` (§74a, plus §74a2 catching stale lower-count prose anywhere), every `should_skip <cat>` in `pre_tool_use.sh` vs the SPEC §7 escape-category enumeration (§74b), and the tier-2 label count vs `ensure_v3_labels.sh` + the inline `directive`/`initiative` (§74c) — so the "enumerable-facts-as-prose" drift class (which caused #238 and several spec-internal-sweep findings) now fails smoke instead of silently rotting. Also reconciled the drifted enumerables this guards: the agent count (6→9 across §3.1/§8/§16/§17), the §7 escape-category list (added the 6 missing `directive-review`/`proposed-protect`/`trusted-filer-mutate`/`label-parent-consistency`/`initiative-readonly`/`release-review`), the §2.1 audit-category list (added `activation`/`directive-reflect`/`directive-exec-count`), the §5.19 stale "10-label" reference, the §6.1 `bypass-suspect` token-vs-category note, and the `issue_type.sh` `safe_source` fail-policy note (added the M1 initiative predicates + matcher). (#267)

### Changed

- Renamed the `directive-reviewer` subagent to `activation-reviewer` and made it type-neutral (dispatches by Issue type label: Directive vs Execution rulebook); verdict vocabulary unchanged. (#170)
- Generalized the `directive-protect` branch-creation hook to `proposed-protect`: it now blocks `git checkout -b` against any `status:proposed` Issue (run `/activate` first) as well as any Directive. (#171)
- Repurposed the label-free-filing workflow (`auto-needs-triage.yml` → `auto-status-proposed.yml`): raw GitHub-UI filings now receive `status:proposed`+`task` and enter the `/activate` gate (closing the raw-channel validation-bypass hole), and the dormant `needs-triage` label is retired. Also fixes a #172 bug where the `awaiting-author` label description exceeded GitHub's 100-char limit. (#179)
- Reconciled the CLAUDE.md "Backbone" autonomy-ceiling line with SPEC §5.7.1: it no longer reads as an unqualified "stops at PR-ready" hard ceiling, but states PR-ready is the ceiling *by default* (`attended`) and that `unattended` deliberately extends it through the `/ship` CI-wait → merge-or-park terminal step — removing a contradictory boundary signal the auto-mode classifier had cited to deny an `unattended` merge. (#201)
- Synced the satellite docs (`.claude/CLAUDE.md`, `README.md`, `README.ko.md`, `docs/DIR_MODE_FLOW.md`) to the live Initiative tier and applied the thin-pointer discipline: `DIR_MODE_FLOW.md`'s duplicate state-machine diagram collapses to a pointer at SPEC §2.1 (one diagram per fact), and its drifted prose (`/activate-directive` as a live step, "two-refine escalation", "10-label set") is reconciled to the current contract (N=3 reviewer self-escalation, §1.7 label roster). EI-D of doc-integrity Directive #264. (#268)
- README (EN + KO) restructured into a lean ~100-line front-door; the full config-toggle catalog moved to `docs/CONFIG.md`, and drift fixed (smoke assertion count 547, SPEC line count ~2,000, hook summary now names `merge-strategy` + `label-parent-consistency`, `docs/DIR_MODE_FLOW.md` linked). (#297)

### Removed

- Retired the triage classifier subagent and deprecated `/triage` to a one-cycle alias for `/activate` (its `status:proposed` gate is now `/activate`; stale-discussion surfacing moved to `/activate` batch mode). The `needs-triage` label is kept but dormant pending Phase-2 external-inbound automation. (#173)

### Fixed

- `/file-directive` skill now captures `Priority` (P0–P3) at filing time and emits a well-formed `directive-file/created` audit_log line — closes the audit-format gap that allowed malformed reasons through. SPEC §18.6 + `check-changelog.yml` header now document GitHub's workflow-modification policy. (#135)
- `/file-directive`'s `P<N>` priority label now works: `P0`–`P3` are created by the canonical `ensure_v3_labels.sh` (tier-2 set, matching SPEC §0.4 which already listed them but the script never created them), de-duplicated from `onboard_target.sh`'s inline block, and `/file-directive` degrades gracefully (warns + files without the label) if it's absent. (#185)
- README reconciled with the merged v3 / activation-gate work: Quick start and both dir-mode flows now show the mandatory `/activate <N>` step (every Issue is filed `status:proposed` and `proposed-protect` blocks `/work-on` until activation), the deprecated `/activate-directive` alias is replaced by canonical `/activate`, the dir-mode substrate section reflects the 4 gh-created Project fields (Type/Status/Priority/Parent) + user-added Iteration with `/onboard-dir-mode` as the canonical installer, the `proposed-protect` hook bullet covers any `status:proposed` Issue, and the SPEC line-count reference is refreshed. (#193)
- `task` and `execution` Issues are now classified consistently: `activation-reviewer` applies parent-fit only to the `execution` label (a `Parent Directive:` marker on a `task`/`bug` is flagged as a relabel-or-drop smell), and `/file-issue` derives the type label deterministically (parent→`execution`, standalone→`task`, `--quick`→`bug`) instead of leaving it to discretion — closing the unattended-mode path that mislabeled a standalone `task` as a parented `execution`. (#197)
- Scoped the `force-push` hook matcher so it no longer blocks force-push unconditionally: force-push to an explicitly-named non-protected branch (`git push --force-with-lease origin <branch>`) is now allowed — the normal tail of the rebase-pull flow (SPEC §13) — while a force-push naming a protected branch (`main`/`master`/`release/*`), or a bare/remote-only force-push with no explicit target, is blocked. The bare form is blocked rather than inferred from the current branch because a no-refspec push's real destination is config-dependent (`push.default`/upstream), so the branch name isn't a safe proxy; the block message tells you to name the target. Escape: `SKIP_HOOKS=force-push`. (#204)
- Fixed the SPEC §7 escape hatch being non-functional through the live Claude Code Bash tool: the leading `SKIP_HOOKS=…` env-prefix is consumed by the harness as the subprocess environment and never reaches the hook, so matchers blocked even when an escape was requested. Added a **trailing-sentinel** form — `<command>  # claude-eng:skip=<category>[,…] reason=<why>` — that survives the harness (a `#`-comment stays in the command and is ignored by the executed shell) and is the supported in-harness escape. It is parsed from the raw command before normalization, namespaced so an ordinary comment isn't mistaken for an escape, one-shot, and audited via the existing escape path; the leading env-prefix form still works where it arrives in-string and wins if both are present. (#206)
- Fixed the staged-secret commit check failing **closed** when `secret_scan.sh` is missing: an unguarded `scan_staged_secrets` call meant an undefined predicate (e.g. the session-restart helper-miss `safe_source` is designed to degrade gracefully) made `command not found` block **every** `git commit`, forcing `SKIP_HOOKS=secret` on each one — the opposite of the SPEC §6.1 fail-policy table's documented fail-open. The call is now guarded with `command -v` like its sibling matchers, so a missing helper skips the secret arm (fail-open) while `safe_source` still logs the `helper-missing` warn. (#213)
- Fixed the documented Directive **unblock** path being a dead end: a Blocked Directive is Active (`directive` + `status:blocked`, no `status:proposed`), so `/activate <N>` errored "already Active" before reaching the reviewer, and even on a pass it only removed `status:proposed` — never `status:blocked`. `/activate` is now blocked-aware: it treats `status:blocked` as a re-activatable precondition, re-runs `activation-reviewer`, and on `pass` removes whichever of `status:proposed`/`status:blocked`/`awaiting-author` are present (so the mirror reconciles Status). Batch mode still does not auto-unblock (single-mode `/activate <N>` only). (#215)
- Fixed the `/resolve-discussion --no-action` close being broken end-to-end: it ran `gh issue close --reason not_planned`, but `gh` accepts only `{completed|not planned|duplicate}` (the **space** form) and rejects the underscore — and the backing `trusted-filer-mutate` discussion arm matched the underscore too, so the gh-valid `"not planned"` form was wrongly blocked as a bare close. Standardized on the gh-valid space form across the command, SPEC §5.19, and the activate.md note; the hook matcher now tolerates both forms so it is never itself the blocker. (#216)
- Fixed the escape-hatch reference docs routing in-harness blocks to the non-functional leading `SKIP_HOOKS=…` form: ESCAPE_HATCH.md, README.md/README.ko.md, and TROUBLESHOOTING.md now present the **trailing sentinel** (`<command>  # claude-eng:skip=<category> reason=<why>`) as the in-harness escape (the leading env-prefix is consumed by the harness as subprocess env and never reaches the hook, #206). Added the `label-parent-consistency` category to ESCAPE_HATCH.md and a row to TROUBLESHOOTING.md, and replaced the stale `*test*`/`*example*` `.shellsecretignore` glob defaults in CLAUDE.md and SPEC §6.1 with the actual component-aware globs. (#217)
- Hardened four low-severity robustness/idempotency gaps in maintenance tooling: `release_consolidate.sh` now fails loudly (non-zero) if a `git rm`/`git add` in the fragment-consume loop fails (was swallowed under `set -uo pipefail`, reporting false success); `ac_closeout.sh`'s detect+convert regexes dropped the trailing-space requirement so they cover the same degenerate `- [ ]` lines the merge gate blocks on (was a gate-vs-remedy deadlock); `cwd_guard.sh` normalizes a trailing slash on registry entries in both scope loops (a trailing-slash entry silently dropped its path from scope — a fail-open on the scope guard); and `migrate_v3.sh`'s header no longer claims snapshot idempotency it never had (second-resolution timestamp). (#218)
- Fixed `trusted-filer-mutate` over-blocking the sanctioned `/resolve-discussion --no-action` path: a `--reason "not planned"` close on a `discussion`-labeled Issue authored by a trusted filer (OWNER/MEMBER/MAINTAINER/COLLABORATOR — the common maintainer-filed case) was short-circuited past discussion classification into the trusted-filer block, forcing a `SKIP_HOOKS` escape on a documented frictionless close. Discussion-tier is now classified first and independent of filer trust: a discussion close allows `--reason completed`/`"not planned"` regardless of filer and blocks bare/`duplicate`; the trusted-filer block applies only to non-discussion Issues. (#236)
- Closed the `--repo owner/name` flag-form half of the `trusted-filer-mutate` cross-repo fail-open (completes #231, which scoped only the URL selector): a `gh issue close <N> --repo OTHER/repo` close-as-not-planned/duplicate of a foreign trusted-filer Issue resolved `tf_repo` empty and evaluated trust/discussion against the *current* repo, so it proceeded unblocked when current-repo #N was untrusted or absent. The close arm now parses an explicit `--repo owner/name` (or `[HOST/]owner/name`) flag when no URL-derived repo is present and uses it for the `is_trusted_filer` / discussion-label lookups; bare-number (no flag) and URL forms are unchanged, and an unparseable `--repo` value fails soft to the current repo. (#237)
- `onboard_target.sh --tier` with no value now prints a usage diagnostic and exits 2 instead of crashing with `$2: unbound variable` under `set -u`. (#238)
- Documentation/test hygiene cluster (LOW): corrected the `/onboard-dir-mode` tier-2 label count (10/11 → the canonical 12, adding the missing `execution` label); fixed `/block-directive`'s unblock hint to reference the canonical `/activate` and "on a `pass` verdict"; corrected a stale comment claiming the trusted-filer selector regex sees a non-shlex command string (`parse_env_prefix` shlex round-trips it when python3+jq are present); and isolated the `is_trusted_filer` cache per smoke section so a rerun re-exercises the gh-query path. (#238)
- Closed the `-R` short-alias half of the `trusted-filer-mutate` cross-repo fail-open (completes #237/#231): `gh issue close <N> -R owner/repo` (gh's documented short alias for `--repo`) was not parsed by the close arm, so `tf_repo` stayed empty and trust/discussion lookups resolved against the current repo — a foreign trusted-filer close-as-not-planned/duplicate via `-R` proceeded unblocked when current-repo `#N` was untrusted/absent. The close arm now parses `-R owner/name` (and `-R=owner/name`, `[HOST/]owner/name`) alongside `--repo`; bare-number/URL/`--repo` forms unchanged, unparseable values fail soft to the current repo. (#242)
- Corrected a CLAUDE.md doc drift: the "What hooks enforce" bullet conjoined the Edit/Write and `rm`/`mv`/`cp` matchers under one "two carve-outs" sentence, implying the `$HOME/.claude/` + `$CLAUDE_ENG_SHELL_ROOT/` carve-outs let forced destructive commands through. They don't — the destructive-command check has no carve-out (SPEC §6.1 scopes the carve-outs to the Edit/Write rows only), so a forced `rm`/`mv`/`cp` under `$HOME/.claude/` is still blocked (use `SKIP_HOOKS=out-of-scope`). Doc now matches SPEC + runtime; smoke §60g guards the alignment. No runtime behavior change. (#243)
- Made SPEC §1.7's `Parent Initiative` "symmetric / consumed-by" claim true where it should be and honest where it shouldn't (post-v2 spec-internal trace finding C1). The `issues-to-project-mirror` workflow now derives a Directive's Project `Parent` field from **either** line-1 marker — `Parent Directive: #N` or `Parent Initiative: #N` — so an Initiative-parented Directive is no longer second-class in the derived view (the `Parent` field is free-text, so the referent type is immaterial; `Parent Directive` stays a strict superset). `/reflect` and `dir-mode-post-merge` are deliberately **unchanged** and the §1.7 claim is scoped accordingly: they consume the *closing Execution Issue's* `Parent Directive` marker, and an Execution Issue is structurally never Initiative-parented (parent-XOR; a Directive never branches nor becomes a PR `closingIssuesReference`), so matching `Parent Initiative` there would be dead code — Directive→Initiative upward feedback flows through `/initiative-feedback` (§5.22). EI of doc-integrity Directive #264. (#262)
- Pinned four under-specified Initiative-tier integration invariants surfaced by the post-v2 spec-internal trace (C2–C5): SPEC §1.7 now states the Directive **create-path split** (`/file-directive` is MISSION-parented; `/consume-initiative` is the sole producer of `Parent Initiative`-marked Directives) and that an Initiative is **outside the 4-state lifecycle + `status:*` taxonomy**; `/activate` (+ SPEC §5.12) **refuses an `initiative`-labelled Issue** via a tri-state type resolution aligned with `activation-reviewer`'s §4.9 dispatch (closing the binary-vs-tri-state mis-typing gap); and SPEC §6.1 documents that `initiative-readonly`'s block beats `trusted-filer-mutate`'s `--reason completed` allowance on an Initiative close, by the **block-is-terminal** rule (not matcher ordering — verified against `pre_tool_use.sh`). smoke §75a–d lock all four. EI of doc-integrity Directive #264. (#263)
- SPEC internal-drift purge (EI-A of the doc-integrity Directive #264): the §2.1 Directive-lifecycle diagram, state table, and prose now use the canonical `/activate` (not the deprecated `/activate-directive` alias, which remains documented only at §5.12/§5.18); the §1.7 command list adds `/consume-initiative`/`/initiative-feedback` and demotes the aliases; dangling `§5.x` cross-refs resolve to §5.12; the `§1.7:309` line-number refs are de-fragilized; the §7 ac-closeout step ref is corrected (7.5→7.6; 7.5 is the unrelated checklist audit); the §1.7 hierarchy diagram no longer annotates the now-live Initiative tier "forthcoming"; and the §6.1 AC-closeout Type-awareness framing is corrected to all-or-nothing (skip only when *every* closing Issue is a Directive). `block-directive.md` alias refs updated. Smoke §61c guards the §2.1 alias purge. (#265)
- §4.9.1 activation-reviewer spec resynced to its SSOT file + Issues-only/Project invariant restored (EI-B of doc-integrity Directive #264, sweep S4 + S2 + #6): §4.9.1 "When" now lists all five caller paths (/file-directive, /activate, /revise-directive, /complete-directive, /consume-initiative via the Initiative rulebook); the stale "parent Goal" is now "MISSION fit (or a Parent Initiative marker)"; the proposal-review active-Directive list reads Issues (`gh issue list`), not the lagging Project (`gh project item-list`) — restoring the §1.7/§5.11 Issues-as-SSOT invariant; the parent-kind alignment check (#254) is added to the §4.9.1 Checks list and §5.16's "five proposal checks" is corrected. §5.2 now states /file-issue writes the `Parent Directive: #N` body marker and the mirror *derives* the Project Parent field (not a direct Project write). §2.1 documents that the 4-state machine is Directive-specific and discussion/initiative Issues sit outside it (their mirrored `Status=Active` is a derived-view artifact). §5.20 step 8 clarifies the on-merge tag+Release is maintainer-run, past the §5.7.1 unattended ceiling (not an unattended continuation). The three activation-reviewer-path commands (file-directive/revise-directive/consume-initiative) now defer to the reviewer's N=3 self-escalation instead of a contradictory "two consecutive revise". (#266)
- Closed three selector-normalization bypasses in the dir-mode enforcement matchers (audit Theme A, EI of Directive #275). The `label-parent-consistency` matcher now resolves its `gh issue edit … --add-label` selector via a shared `resolve_gh_issue_target` helper that is tolerant of selector **form** (bare / quoted / **URL** — A1, previously a silent bypass) and flag **order** (the positional issue arg need not immediately follow `edit` — A2), and threads a `-R`/`--repo` (or URL-embedded) **target repo** into the marker/label predicates so a cross-repo relabel resolves against the command's actual repo (A3); the predicates' new optional `repo` arg also keys their per-session cache by `owner/name`, so a foreign `#N` lookup can't poison the current repo's same-numbered entry. `initiative-readonly` migrates to the same helper (gaining flag-order tolerance) but deliberately stays **current-repo-only** — cross-repo Initiatives are out of scope (§1.7), so it governs only same-repo Initiatives. `trusted-filer-mutate`'s own parser is untouched. (#276)
- Closed a destructive-command bypass (audit Theme B, EI of Directive #275): the `rm`/`mv`/`cp` force/recursive out-of-scope block could be evaded by placing the flag **after** the operand — `rm /path/outside-registry -rf` (valid GNU syntax) — because the entry pre-filter required the flag to follow the verb with only other flags between. The pre-filter now recognizes the flag anywhere in the verb's argument run, including after operands, while still **stopping at a pipeline boundary** (`;` `|` `&`) so a flag belonging to another command (`rm ./ok && ls -rf`) does not trigger it (#212 cross-command anchoring preserved), and matching the flag as a whole token so a `-rf` inside a filename is not a false trigger. `check_destructive_args` (the in-scope/out-of-scope decision) was already operand-position-agnostic; only the entry pre-filter needed widening. In-scope permutations (`rm ./build -rf`) remain allowed. (#277)
- Fixed a token-boundary over-match in the `label-parent-consistency` matcher (audit Theme C, EI of Directive #275): its label regex terminated the type-token alternation with `([^a-z]|$)`, but `-` and `_` are `[^a-z]`, so a custom label that merely *starts with* a type token (`--add-label directive-foo`, `task_old`) over-matched the bare token and falsely tripped the matcher (e.g. blocking a `directive-foo` relabel as if it were the `directive` type-key). The terminator is now a true label boundary — comma / whitespace / quote / end — so suffixed labels no longer over-match, while the bare token, comma-joined lists, and the already-excluded letter-suffix case (`executionish`) are unaffected. The sibling `is_directive_issue`/`is_initiative_issue` label greps were already comma-anchored (`(^|,)directive(,|$)`) and needed no change. (#278)
- Test-integrity sweep (audit Theme E, EI of Directive #275): documented the two vacuous-pass anti-patterns — comment-satisfiable greps and silent skips on an absent target — as a discipline note in `scripts/test/smoke.sh`'s header (so the class is recognizable in review, parallel to the §74 enumeration discipline), and added a **count-guard** to §54g. The guard immediately surfaced a stale assertion: §54g claimed "all 6 template+workflow YAML files parse cleanly" but `[ -f ] || continue` over the glob meant an empty match would pass having checked nothing — and the real count had drifted to 7 (`discussion.yml` was added without updating the assertion). Reconciled to 7. The two instances found earlier (§57e comment-satisfiable grep, §57i silent missing-file skip) were already hardened under #262/#276. Theme F (parser limitations in `resolve_gh_issue_target`) filed as the deferred follow-up #283. (#279)
- Hardened the `resolve_gh_issue_target` selector parser (deferred follow-up from #276): it now tokenizes **shell-aware** via python3 `shlex` (mirroring `check_destructive_args`, with a `read -ra` fallback), so a quoted flag value containing an interior digit — `gh issue edit --body "fixes 99 things" 7 …` — no longer has its `99` mistaken for the positional issue selector (it correctly resolves `7`); and it normalizes a host-prefixed repo (`--repo github.com/o/r` → `o/r`) so the downstream `owner/name` derivation isn't `github.com`/`r` (which had caused a wrong-repo / fail-open lookup). On an unparseable command (unclosed quote — which would not execute in a real shell anyway) the helper emits an empty issue so the caller fails open. Both limitations were fail-safe before this fix (wrong-same-repo / fail-open, never a cross-target bypass); this closes them for accuracy. (#283)
- Closed the reviewer working-tree-isolation gap (surfaced during the v0.3 run): the read-only-by-intent subagents (`code-reviewer`, `security-reviewer`, `issue-reviewer`, `plan-reviewer`, `activation-reviewer`, `explorer`) share the parent session's working tree and carry `Bash`, so a tree-mutating git command inside one (`git checkout <base> -- <path>`, `restore`, `stash`, `reset`, `add`) silently reverts or stages the parent's uncommitted work — observed once as a post-commit reversion of `scripts/test/smoke.sh`. Two layers now prevent it: (a) the SPEC §1.5 norm makes **worktree isolation** the canonical way to invoke these subagents, and (b) each such agent's prompt is constrained to **read-only git only** (compare a base via `git diff <base>...HEAD` / `git show <ref>:<path>`, never `git checkout <base> -- <path>`). smoke §77 asserts the constraint is present in all six agent files. The structural backstop (merges build from the pushed commit, not the working tree) means a post-commit reversion never ships; the norm adds "git status before each commit/merge" as the catch-all for the pre-commit window. (#285)
- Added a `merge-strategy` PreToolUse matcher that enforces SPEC §5.7.1's no-ff merge mandate at runtime: `gh pr merge` to the **default branch** is blocked unless the strategy is `--merge` (`--squash`/`--rebase`/a bare merge → block, because a squash collapses the Doc→Test→Code arc into a single commit on the trunk — §1.2's load-bearing artifact). The policy was documented but unenforced, so manual `gh pr merge --squash` had been silently collapsing PR history on `main`. Squash is still **allowed** on a non-default base (topic-branch consolidation, §10.5) — the block keys on the live default branch (`gh repo view --json defaultBranchRef`), not `PROTECTED_BRANCH_PATTERN`. The matcher parses strategy first and allows `--merge` (incl. `--auto --merge`, the `/ship` form) silently with no `gh` calls (preserving the §39d silent-happy-path invariant); only a non-`--merge` strategy triggers the base/default-branch lookup, which fails open (`audit_log warn`) if `gh` can't resolve. Independent of the `ac-closeout` arm (own `should_skip` category). Escape: `SKIP_HOOKS=merge-strategy`. smoke §78a–h. (#288)
- Hardened the `merge-strategy` matcher's command parsing into a gh-flag-aware token walk (`parse_gh_merge_argv`, shell-aware via `shlex` with a `read -ra` fallback), closing three matcher-vs-gh mismatches from #288's security review (all below SPEC §6.1's guardrail threshold, but real): (A) a `--merge` appearing inside a `--body`/`--subject` *value* is no longer read as the strategy — strategy is the explicit flag token, not a substring, so `gh pr merge N --squash --body "see --merge"` correctly blocks; (B) the PR is the first *positional* token (or a `.../pull/N` URL), skipping value-taking flags' values, so `gh pr merge --body 7 --squash 5` resolves PR 5 (not 7); (C) the short merge flag `-m` is now recognized as compliant (it was wrongly blocked), and `-s`/`-r` short squash/rebase are blocked. The core §5.7.1 enforcement (block squash/rebase/bare to the default branch, allow `--merge`, allow squash on a non-default base) is unchanged; this only sharpens how strategy and PR are read from the command. (#290)
- `/file-issue` now captures a priority (`P0`–`P3`) and applies it as a label at create time, bringing it to parity with `/file-directive` — eng-mode issues no longer land priority-less (a priority-less backlog can't be triaged). Source order: an explicit `--priority P<N>` flag → else ask the user (attended) → else default `P2` (unattended, the same default `/file-directive` uses). The `P<N>` label is graceful-degradation-guarded (applied only if present on the target; else a warn and file-without-it) and the priority is recorded in the `issue-file` audit line so it survives a degraded label. An Execution Issue carries its own priority (it does not silently inherit the parent Directive's). SPEC §5.2 + `file-issue.md` document the contract; smoke §79 guards it. (The session's own task issues #283/#285/#288/#290 — filed before this — were backfilled by hand.) (#291)
- Made the Co-Authored-By trailer **version-agnostic** (SPEC §10.2). `coauthor_trailer` now emits `Co-Authored-By: Claude <noreply@anthropic.com>` instead of the version-pinned `Claude Opus 4.7 (1M context) …`, which had gone stale once the model advanced to 4.8 — pinning a version guaranteed the trailer drifts at every model bump. The toggle (env → state → default `on`) and all other §10.2 semantics are unchanged; SPEC §10.2's example + prose updated to match, and smoke §33e asserts the emitted line carries no model version so it can't silently re-drift. (#294)

### Security

- Fixed the trailing `# claude-eng:skip=` escape sentinel firing when the `#` is inside a quoted argument rather than a genuine unquoted shell comment: ordinary quoted text (a commit message, a PR-body paste, audit output quoting the sentinel) could silently set `SKIP_HOOKS` and disarm every matcher with a falsified audit reason while the shell ran the command intact. The sentinel is now honored only when its `#` is a real comment token, resolved with the same `python3`/`shlex` machinery the leading env-prefix uses; `python3` absent → sentinel not honored (fail-safe, enforcement stays armed). (#208)
- Closed a commit-gate bypass: `git commit --allow-empty` was excluded from the umbrella commit matcher, so it skipped the protected-branch block, the Conventional Commits subject check, the staged-secret scan, and the lint gate in one flag. The matcher now fires for empty commits too (branch + commit-format still apply; secret/lint are no-ops on an empty staged set). (#209)
- Fixed the shell-self-modification carve-out (`$CLAUDE_ENG_SHELL_ROOT/`) doing an early `exit 0` before the sensitive-file check, so an Edit/Write of a `.env`/`*.pem`/`credentials*` file under the shell root was not blocked — contradicting the documented "sensitive-file check fires under both carve-outs" contract. The carve-out now uses a flag (mirroring the `$HOME/.claude/` carve-out) and falls through to the sensitive-file check. (#210)
- Closed a declassify-guard bypass: the `trusted-filer-mutate` matcher blocking `gh issue edit <N> --remove-label directive` only matched the space-separated form, so the `=`-separated form (`--remove-label=directive`, valid `gh` syntax) silently dropped the `directive` label and de-classified a Directive without the required human confirm. The matcher now accepts both separator forms (optionally quoted) and a comma-joined value list where `directive` is any element (`--remove-label other,directive`, which gh honors), with a word-boundary tail so a longer label like `directive-foo` does not over-match. (#211)
- Hardened three hook matchers that recognized only one surface form of a command/label: the destructive-path guard now fires on `rm`/`mv`/`cp` with force/recursive flags in any form (`-r -f` separated, `--force`/`--recursive` long, `-i -rf` non-first — previously only a clustered `-rf` immediately after the verb); the `label-parent-consistency` matcher now detects the gated label anywhere in a comma-joined `--add-label` value; and `is_directive_issue` now anchors on comma-list boundaries (`(^|,)directive(,|$)`) so a label like `non-directive` is no longer mis-classified as a Directive. (#212)
- Broadened the staged-secret scanner's OpenAI key pattern to catch current-generation key formats (`sk-proj-…`, `sk-svcacct-…`, `sk-admin-…`, which contain `-`/`_`). The old `sk-[A-Za-z0-9]{40,}` stopped at the first `-` after the prefix, so the length floor was never met and modern keys passed the scan as a false negative. The `{40,}` floor is retained to limit false positives on short `sk-` identifiers. (#214)
- Closed a `trusted-filer-mutate` selector-evasion bypass: both sub-arms anchored the issue selector as a bare `[0-9]+`, so `gh issue edit https://…/issues/<N> --remove-label directive` and `gh issue close "<N>"` (URL or quoted-number selectors, both accepted by gh) evaded the matcher entirely — silently declassifying a Directive or auto-closing a trusted-filer Issue. The matcher now recognizes bare-number, quoted-number, and URL selectors, and the close arm normalizes the selector to a pure issue number before the `is_trusted_filer` / discussion-label lookups (which require a bare number) and the block message. (#223)
- Made the `trusted-filer-mutate` close arm repo-aware (amends #223): a foreign-repo URL selector (`gh issue close https://github.com/OTHER/repo/issues/N`) now resolves filer-trust and the discussion label against the URL's `owner/name` (via `--repo`) instead of the current directory's repo, so a foreign-repo close is evaluated against the foreign repo's issue rather than a same-numbered local one. `is_trusted_filer` gained an optional `owner/name` arg (current-repo behavior unchanged when omitted); a bare-number selector is unchanged and an unparseable URL repo fails soft to the current repo. (#231)
- Closed a sensitive-file guard evasion: the Edit/Write `.env`/`*.pem`/`credentials*` check matched only the **lexical** basename of the raw target, so a symlink with an innocuous name pointing at a sensitive file (e.g. `ln -s ~/.ssh/id_rsa ./innocent` then editing `./innocent`) wrote through the link unblocked. When the final path component is a symlink the check now also matches the `python3`-realpath-resolved basename (blocks if either the lexical or resolved name is sensitive); `python3` absent/unresolvable falls back to lexical-only (fail-soft). TOCTOU and hardlinks remain out of scope. (#234)

## [0.1.0] — 2026-05-26

Inaugural tagged release. Covers `init` through `v0.1.0` as a single block (per Directive #128 non-goal — no per-milestone backfill).

### Added

- Initial shell scaffolding — entry binary, bootstrap, injection, and registry of registered target paths. (#1)
- Hook system with five Claude Code event hooks (`PreToolUse` / `PostToolUse` / `Stop` / `UserPromptSubmit` / `SessionStart`) and fourteen helpers under `.claude/hooks/`. (#3)
- PR / issue / mission / ADR templates with section ownership and curate-on-commit discipline. (#5)
- Ten subagents — `planner`, `explorer`, `doc-writer`, `test-writer`, `code-reviewer`, `security-reviewer`, `issue-reviewer`, `plan-reviewer`, `directive-reviewer`, `triage-reviewer`. In `unattended` mode the six reviewers substitute for human-confirm checkpoints. (#7, #44)
- Slash commands — `/onboard`, `/file-issue`, `/work-on`, `/sync-pr`, `/status`, `/review`, `/ship`, `/adr`, `/audit`. (#9)
- CI workflow (`.github/workflows/ci.yml`) with syntax (`bash -n` + `shellcheck`) and smoke jobs across a dual-OS matrix (`ubuntu-latest` + `macos-latest`). (#13, #17, #21)
- Dir-mode v0 — Final Goal → Directive → Execution Issue layer with `directive-reviewer` subagent, five dir-mode commands (`/file-directive`, `/activate-directive`, `/list-directives`, `/link-directive`, `/complete-directive`), and type-aware engineering hooks. (#42, #43, #44, #45, #46)
- Dir-mode commands `/revise-directive` and `/block-directive` for in-place Directive body edits and Blocked-state lifecycle. (#78, #80)
- `/reflect` skill posts a Directive-progress comment on the parent Directive after each Execution PR merges. (#47, #57)
- `/triage` skill — binary accept/reject classifier for `needs-triage` / `status:proposed` Issues, backed by `triage-reviewer`. (#94)
- Discussion-tier Issues (`/discuss`, `/resolve-discussion`) — friction-free filing for "weird but not a bug" observations; bypasses the rationale-triad gate. (#112, #116)
- `/onboard-dir-mode` tier-aware target-substrate installer (tier 1 / 2 / 3) — installs the 10-label v3 set, Issue templates, workflows, and Project v2 into adopting target repos via a PR (ADR-0004). (#114, #118)
- User-global memory carve-out — Edit/Write under `$HOME/.claude/` is allowed by the registry / scope guard so the auto-memory tier works without weakening the shell's user-global-isolation contract. (#91)
- Filer-aware mutation invariants — `trusted-filer-mutate` hook arm blocks `gh issue close` on a trusted-filer Issue without `--reason completed` and blocks `gh issue edit --remove-label directive` on any filer. (#95)
- `dir-mode-post-merge` GitHub Actions workflow runs Directive-status maintenance (Active → Completed via `/complete-directive`, Status field mirroring) on every Execution PR merge. (#63)
- `claude-eng --version` short-circuited before registry resolution, sourcing from the top-level `VERSION` file with `git describe --tags` fallback. (#123)
- Annotated tag `v0.1.0` marking the first tagged release (`v` + semver convention locked by Directive #122). (#126)

### Changed

- Dir-mode v3 substrate reframe (ADR-0003) — collapsed the Goal-as-Item Project artifact into doc-as-code `MISSION.md`; Issues are the SSOT, Project v2 is a derived view; Directives use a four-state lifecycle (Proposed / Active / Blocked / Completed) encoded as labels with the Project Status field mirrored by `issues-to-project-mirror.yml`. (#96)
- Audit log format v3 — structured JSON-line format with stable field ordering and category/event tokens audit-callers can grep. (#108)
- AC-closeout matcher skips when every closing issue carries the `directive` label — Directives complete via `/complete-directive`, not AC checkboxes. (#46)

### Fixed

- macOS 3.2 bash compatibility — removed `globstar` and equivalent bash-4-only patterns; smoke + syntax run identically on Linux and macOS legs. (#17)
- Shellcheck warnings across hooks and helpers — real bugs plus justified inline silences in `.shellcheckrc`. (#21)
- Secret-scan emits per-line `<file>:<line>: <pattern-id>` markers and honors a `.shellsecretignore` allow-list at the target-repo root (gitignore-narrow). (#25)
- `SessionStart` banners cover two silent-no-op states: shell injected without `CLAUDE_ENG_SHELL_ROOT` set, and `CLAUDE_ENG_SHELL_ROOT` set but `hookrt.sh` missing. (#23, #37)
- `safe_source` utility centralizes hook-to-helper and helper-to-helper sourcing; missing helpers fail-open with one `audit_log warn <category> helper-missing` record. (#31, #34, #36)
- Matcher pass-through invariant — `pass_through_trace` audits every `pre_tool_use.sh` matcher that fell through without a decided state; high-frequency happy paths call `mark_allow` for silent ack. (#33)
- AC-closeout race / misfire fixes — split `gh pr merge` and `ac_closeout.sh` invocations to avoid GitHub API propagation latency. (#83, #29, #31)
- `setup_project.sh` reconciles `SINGLE_SELECT` options additively on re-run; preserves user-added options. (#76)
- Post-v3 drift sweep — README subagent counts, MISSION-fit narratives, and skill metadata aligned to the v3 substrate flip. (#120)

### Security

- Sensitive-file editing (`.env`, `*.pem`, `credentials*`) blocked by `PreToolUse` hook regardless of carve-outs. (#3, #25)
- Staged-diff scan for known secret formats (AWS, GitHub, GitLab, OpenAI/Anthropic, Slack tokens, password literals). (#25)
- Shell audit log never persists diff bodies — only structured records of decisions, categories, and reasons. (#3)

[0.1.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.1.0

[0.2.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.2.0

[0.3.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.3.0

[0.4.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.4.0

[0.5.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.5.0

[0.6.0]: https://github.com/ilgyu-yi/claude-eng-shell/releases/tag/v0.6.0

[0.7.0]: https://github.com/ilgyu-yi/ghjig-claude/releases/tag/v0.7.0
