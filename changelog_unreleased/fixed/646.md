- Worktree isolation separates a subagent's git tree but not the session scratchpad, which every subagent shares by path — so concurrent reviewers in a §4.11 N-way vote could overwrite each other's execution harnesses under conventional names, and a majority rule would convert that correlated contamination into confidence; SPEC §1.5 now names the scratch axis beside the tree axis, §4.11 records that independence covers the execution environment, and both reviewer contracts carry the `mktemp -d` recipe with a `PATH`-residency assertion, an artifact hash-pin re-verified after the run, and a fresh directory on any re-run. (#646)
