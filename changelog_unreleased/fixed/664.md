- Every `python3 -c` invocation under `.claude/hooks/` now runs isolated (`python3 -I -c`), closing the measured cooperative-plant bypass: a module file planted inside the registered project (a write the shell's own guards permit) could shadow a non-preloaded stdlib import (`re`, `shlex`) and forge a matcher verdict — end-to-end, a cooperative `./re.py` made a force-push to a protected branch pass through a healthy interpreter. SPEC §6.1.2 records the 12-site classification (imports, preloaded-or-not, what each output decides) with an explicitly empty exemption set; smoke §160i locks the measured bypass behaviorally (cooperative plant + paired negative + same-PATH positive control + broken-plant leg) and §160j locks the uniform contract statically (form-independent: any non-comment `python3 -c`/stdin invocation without `-I` reds). The recovery diagnostics now reproduce the guards' own invocation form. (#664)
