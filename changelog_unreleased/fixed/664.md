- A cwd-planted module no longer forges the git command matchers' input under a perfectly healthy `python3`: the constant-tag frame #660 installed at `strip_command_data` authenticates only that the program *ran to completion*, which a **cooperative** plant does — measured end-to-end, a `./re.py` planted at an in-registry path the shell's own Edit/Write guard permits reduced `git push --force origin main` to a validly framed but elided command and the force-push arm returned rc=0 (rc=2 plant-absent, with an `Edit`-on-`.env` control still rc=2). Every `python3 -c` invocation site under `.claude/hooks/` now carries the literal `python3 -I -c` — the leading position is load-bearing, since a trailing `-I` is an argument to the program (`sys.argv == ['-c','-I']`) and the plant still loads — with the frame/`-I` orthogonality correction, the twelve-site classification keyed by function name, the explicitly EMPTY exemption set, `-I`'s full effect (`-E` + `-s`, not only `-P`), and the per-outcome over-block disclosure including the three sites that fail **open** stated once in SPEC §6.1.2; the recovery diagnostics in `pre_tool_use.sh` and `docs/TROUBLESHOOTING.md` now reproduce the guard's own invocation. (#664)
