- A refused file-based skip token no longer vanishes without a trace: every arm of `_escape_token_honored` that **consumes** a token now writes one `escape/<category>/reject` audit record naming the arm that refused — and the composite parse/bind arm names its five clauses separately, so a token minted by a broken `date` (`created=` present but empty) is no longer indistinguishable from a category mismatch, whose remedy is the opposite; the two non-consuming early returns (unresolvable state dir, absent/unreadable token) stay silent because they never examined a token, the honor path's single `escape/<category>/skip` record is unchanged, and the addition is observability-only — every input refused before is still refused, nothing new is honored, and a failing `audit_log` cannot convert a reject into an honor, with the rule stated once in SPEC §7 beside the on-honor record. (#653)
