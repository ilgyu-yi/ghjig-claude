- The audit trail no longer splits by writer context: `audit_log` resolves its destination unconditionally through a dedicated audit resolver (override seam → `CLAUDE_PROJECT_DIR` → main-worktree top level → self-location — never the env lottery), so a command-emitted record from a plain Bash step, an agent worktree, or an env-unset shell lands in the same per-project `ghjig-state/audit/audit.jsonl` the consumers read; the legacy shared `.claude/audit/audit.jsonl` is demoted to a read-only floor consulted only when the per-project file is absent, `/audit` and the aggregate consumers resolve via the shared reader, and the worktree/env-unset silent-drop shapes are closed. (#725)
