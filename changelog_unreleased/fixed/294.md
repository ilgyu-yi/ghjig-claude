Made the Co-Authored-By trailer **version-agnostic** (SPEC §10.2). `coauthor_trailer` now emits `Co-Authored-By: Claude <noreply@anthropic.com>` instead of the version-pinned `Claude Opus 4.7 (1M context) …`, which had gone stale once the model advanced to 4.8 — pinning a version guaranteed the trailer drifts at every model bump. The toggle (env → state → default `on`) and all other §10.2 semantics are unchanged; SPEC §10.2's example + prose updated to match, and smoke §33e asserts the emitted line carries no model version so it can't silently re-drift.
