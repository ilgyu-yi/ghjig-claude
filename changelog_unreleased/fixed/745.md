- Fix the dir-mode evidence gates on GitHub Enterprise Server: the single GraphQL round-trip behind `activation-evidence` and `completion-evidence` was unpinned, so it resolved gh's default host and both gates failed closed on every GHES target — the Proposed→Active flip and a Directive completed-close were reachable only through the §7 escape. (#745)
