- A degraded or shadowed `python3` no longer forges the destructive-path and git matchers' input: `check_destructive_args` keyed its `shlex` tokenizer on `command -v` presence and exit status, so a banner interpreter's words became the token list and `path_in_scope` absolutised them against `pwd -P` into scope; its word-split fallback leaked every quoted operand form (`rm -rf "/etc/httpd"` rc=0 vs rc=2 healthy); `parse_env_prefix` keyed on `jq`'s exit status, which is 0 with empty output on empty input, so an empty-stdout `python3` blanked `cmd` and killed every Bash matcher upstream (`git push --force origin main`, `git reset --hard`, `git clean -fd` all rc=0); and `python3 -c` put the cwd on `sys.path`, letting a `./shlex.py` planted at an in-registry path the shell's own Edit/Write guard permits forge the decision under a perfectly healthy interpreter. All three now key on output validity, the fallback refuses what it cannot tokenize rather than degrade to a weaker parse, and both delegation sites run isolated — with the inverted fail-closed direction, the scoped add-only sentence, the `-I` requirement and the disclosed per-outcome over-block stated once in SPEC §6.1.2. (#662)
