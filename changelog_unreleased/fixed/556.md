- `scripts/ac_closeout.sh` no longer deadlocks the merge gate and no longer re-pings @mentions: its idempotency guard now mirrors the `ac-closeout` gate exactly (skips only on the strict `## AC closeout (resolved by PR #N)` marker from a trusted-filer comment, so a loose or untrusted lookalike can no longer make the remedy skip while the gate keeps blocking), and it neutralizes `@mentions` in the acceptance-criterion lines it refiles into the closeout comment. (#556)
