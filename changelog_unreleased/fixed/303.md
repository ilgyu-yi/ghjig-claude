- `check-changelog.yml` now fails a PR if **any** added changelog fragment is malformed, not only when *every* fragment is — a single valid fragment no longer masks a malformed sibling. The gate also now enforces the leading `- ` bullet form, and both the gate and `release_consolidate.sh` validate the same five-part contract so the per-PR check can't diverge from the release-time validator (SPEC §18.6). (#303)
