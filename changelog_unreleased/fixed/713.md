- The judged-list reader (`scripts/ghjig_judged_list.sh`) no longer aborts on a zero-padded round token (base-10 normalization; `08` beside `8` is one duplicate round), surfaces its two near-canonical anomaly classes as named stderr lines instead of silence, cleans its temp file on INT/TERM, and — with its two `ac_closeout` filter twins — drops the inert `MAINTAINER` token from the trusted-author literal. (#713)
