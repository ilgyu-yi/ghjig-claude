- The PreToolUse `--no-verify` matcher no longer false-blocks a non-git command whose heredoc/quoted body merely mentions the tokens (e.g. filing an issue *about* the flag) — the arm now strips heredoc data before scanning, matching its sibling arms. (#605)
