- On a GHES target, two non-review-path `gh api` sites left out of #610 now resolve the repo's host instead of gh's default (github.com): `/onboard`'s branch-protection check (was 404/mis-reporting the enterprise repo's protection) is host-pinned via `gh repo view --json url`, and `/release`'s changelog footer link derives the repo host (was hardcoded to `github.com`, and its origin-parse only matched github.com so a GHES release emitted no link at all). (#614)
