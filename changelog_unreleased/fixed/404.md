- Filer-trust resolution (`is_trusted_filer`) now reads `author_association` via `gh api`, fixing silent always-fail on gh versions where `gh issue view --json authorAssociation` is rejected as an unknown field. (#404)
