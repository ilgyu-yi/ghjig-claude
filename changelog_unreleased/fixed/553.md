- Hardened CI guardrails: the Issue→Project mirror no longer blanks the Parent field on a web-UI CRLF or trailing-text marker and degrades gracefully on transient API errors; check-changelog no longer conflates a `gh pr diff` transport failure with a missing fragment and now rejects a fragment whose bullet and `(#N)` ref are split across lines; the bare-`Refs` detector no longer silently drops a directive-scoped Ref past sorted position 5 (#553)
