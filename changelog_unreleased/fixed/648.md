- The four reviewer-gated commands (`/work-on`, `/file-issue`, `/activate`, `/complete-directive`) documented their categorized reviewer-reject audit emission with a bare-relative `safe_source helpers/reviewer_audit.sh`, which resolves only when cwd is `.claude/hooks/`. Run from the project root — where the main assistant runs commands — it failed open (the command proceeded) but the emission was lost: the helper was never sourced, `reviewer_reject_audit` was undefined, the Directive #356 categorized reject record was never written, and a false `helper-missing` warn was emitted for a file present on disk. The four sites now thread the path through the `.claude/ghjig-root` binding symlink, so the categorized reject records land as intended. SPEC §6.1 states the call-form contract (`safe_source`'s first argument is a caller-resolved path, not a bare name), and a smoke arm asserts every documented `safe_source` invocation resolves from the repo root. (#648)
