- `finding-judge`'s judged-list comment shape (header + marker), round derivation, and posting now have a single code home, `scripts/ghjig_judged_list.sh`: both producers (`/review` step 3.5, `/ship` step 1.5) invoke it to post the judged list, the judge's read recipe consumes prior rounds through its `rounds`/`show` modes, and the round is computed by code — `1 + max(round=)` over canonical comments — instead of recomposed from prose. (#711)
