- `finding-judge`'s judged-list comment shape is now stated where the caller composes it: the agent file defines it once as a placeholder-form fixture, the judge renders the header and marker lines for the caller to copy verbatim, and the round derivation is `1 + max(round=)` over canonical comments instead of a marker count. (#711)
