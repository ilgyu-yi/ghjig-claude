- SPEC's audit-record-shape examples now match what `audit_log` actually writes: the §6.1 helper-missing example and the §7 escape example carry the `source` field (added on every record by #361), and the `info` event kind (emitted by `audit_log info` flows) is documented in a new event-kind set so it no longer contradicts the "no new event-type kinds" rule. Doc-accuracy only; no behavior change. (#365)
