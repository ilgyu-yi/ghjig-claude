- Reviewer subagents now pin their artifact to the pushed PR head (a worktree-isolated reviewer is checked out at the base, not the head), and the invoking skill blind-compares each reviewer's independently reported reviewed head against the true head — so a reviewer that reviewed a stale artifact can no longer approve it: a head mismatch (or absent/unconfirmed head) is a fail-closed invalid vote, never counted toward the ship/review majority. (#544)
