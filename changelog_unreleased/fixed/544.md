- A pushed PR can no longer be merged on a stale or unreviewed head: reviewer subagents pin their artifact to the pushed PR head and the invoking skill blind-compares each reviewer's independently reported reviewed head against the true head (a mismatch or absent head is a fail-closed invalid vote, never counted toward the ship/review majority), and an un-skippable pre-merge hook now blocks `gh pr merge` when the local branch has unpushed commits (push-parity) or when the review was skipped (no attestation file, checked with no network) or performed at a stale head (merge-attestation). (#544)
