- An unattended `/ship` no longer parks at the self-review producer step: `/file-review` now writes the sanitized review body itself to the fixed `.claude/ghjig-state/file-review/staging` file and the single already-allow-covered bare `ghjig_file_review_post.sh` wrapper validates and posts it, so no command on the producer path needs an allow entry that cannot be written wildcard-free — one script and one prospective `permissions.allow` entry are removed, while the head-staleness guard is retained and the fail-closed reject set is enlarged and audited. (#633)
