- The protected-push and force-push guards no longer let an unlisted leading git global flag (e.g. `git --no-lazy-fetch push origin main`) drop the `git <verb>` match — `GIT_PREFIX` now tolerates a generic leading `--long[=v]`/`-X` flag run while preserving value-flag consumption (`-c <kv>`). The contrived `-c alias.<x>=<verb>` rename bypass is a documented SPEC §6.1 residual. EI-5 of the hook-enforcement hardening directive. (#503)
