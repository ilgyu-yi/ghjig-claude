- Both fail-closed TTL gates — the `/file-review` publish wrapper's 60s staging-mtime bound and the escape hatch's 60s skip-token bound — now validate the **clock reading** to the same standard as the timestamp it is compared against, so a malformed `date +%s` can no longer make the subtraction fail and skip the freshness check outright, which had let a stale review body reach the public POST and a stale skip token disarm a hook with no audit record; SPEC §5.29 now enumerates the wrapper's fail-closed arms by name rather than by a count that had drifted. (#647)
