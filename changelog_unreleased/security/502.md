- SessionStart now surfaces a **zeroed scope-guard registry** (a present-but-empty `eng-state/registry.txt` silently disables all enforcement via the by-design empty-registry fail-open) with a once-per-session banner + a persistent `registry-zeroed` audit record — so the disarmed state is no longer traceless. The empty-registry fail-open itself is unchanged (no §1.4 flip); the binding-symlink-repoint disable is documented as an accepted residual (structurally undetectable from within). EI-4 of the hook-enforcement hardening directive. (#502)
