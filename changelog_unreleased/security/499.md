- The merge-strategy, ac-closeout, and trusted-filer-mutate PreToolUse gates no longer let a leading `gh` global flag (`gh --repo o/r pr merge`, `gh -R o/r issue close`) walk past their entry anchor — the anchors now tolerate a leading global-flag run while still requiring the subcommand adjacent (and not over-matching a `merge`/`close` word in a `pr create` body). EI-1 of the hook-enforcement hardening directive. (#499)
