- Made the unattended `/ship` classifier (`ship_classify_blocker`) fail closed on a malformed non-empty stdin payload: an unparseable payload now defaults to `hard` (via a `jq empty` input guard) instead of falling through to `clean`, so a garbage payload can no longer route toward an unattended merge — matching the empty-stdin and jq-missing fail-closed guards. (#621)
