- The `merge-review` hook now blocks a covered-form (`gh pr merge --auto --merge --delete-branch`) agent self-merge (PR author == merger, fail-closed on the author/merger lookup) even under `review-gate=bypass`, closing the disclosed-and-accepted residual (§5.7.1) where the static `permissions.allow` classifier exception was the sole guard for that exact form; non-covered shapes (the classifier re-engages) and human-initiated merges stay admitted under `bypass`. (#592)
