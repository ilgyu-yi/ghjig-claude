- The isolated Code-phase **implementer** is now the **default** `/work-on` route (Directive #477 signal-4): `/work-on` dispatches `/implement` for Phase C by default, with a documented opt-out for trivial / one-line / glue edits and orchestrator glue (which stay in the main loop). The formal A/B measurement gate was descoped by maintainer decision — the context-narrowing hypothesis is adopted as operating policy on circumstantial evidence (the convergent mainstream agent pattern + the native subagent context-discard), hedged by fail-open reversibility (a missing implementer path degrades to main-loop authoring; the documented opt-out makes the default revertible) rather than a measured result. `implementer.md` / `/implement` / SPEC §1.2/§3.7/§4.12/§5.28/§1.9 and the smoke §126b/§126c lock are flipped from the opt-in posture to default-with-opt-out (#492)
