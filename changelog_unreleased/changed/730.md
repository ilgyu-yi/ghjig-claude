- Dispatch prompts now carry checkable facts (SPEC §1.5, Directive #637 L4's dispatch-channel completion): a descriptive fact in a subagent prompt is derived-with-command, a stable resolvable pointer, or explicitly labeled unverified — pointer-first (verify once, reference many), with consumer trust legitimate for the first two forms; the six dispatch-composing commands (`/work-on`, `/ship`, `/review`, `/file-review`, `/replan-check`, `/complete-directive`) carry a one-line pointer clause and `.claude/CLAUDE.md` a thin norm line. Advisory by construction — no hook parses prompt text; the reviewer layer is the backstop. (#730)
