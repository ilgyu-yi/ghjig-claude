- CI shellcheck is now version-pinned (`0.11.0`, SHA256-verified fail-closed) and both CI and local runs go through `./scripts/lint.sh`, so "clean locally" predicts "clean in CI"; the per-file split is retained and a Linux peak-RSS flag guards the smoke.sh memory cliff. (#545)
