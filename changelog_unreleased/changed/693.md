- The `_escape_token_honored` TTL fall-open rationale — measurement-mode declaration, three-signature fall-open taxonomy, threat model — now lives in SPEC §7.1 rather than in `escape.sh`'s inline comments, which keep a thin pointer plus the three signature shape-names; four measured claims are corrected in the move (the leading-zero fall-open reach set is only epochs containing an 8 or 9, the measurement discriminator is per-line parsing rather than `bash -c` versus a script file, the `mtime-malformed`/`now-malformed` parenthetical in SPEC §5.29 and `/file-review` gains the over-length limb matching the `-le 11` bound, and `hookrt.sh`'s resolver comment now claims only-script-caller). (#693)
