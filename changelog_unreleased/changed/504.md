- `/work-on` now resolves the repo's default branch (`gh repo view --json defaultBranchRef`) for the `Closes`/`Refs` trailer instead of assuming `main`, so on a `master`/`release`-default repo a PR to the trunk auto-closes its issue correctly (was: bare `Refs` → never closes). Also migrates the `block-directive` / `resolve-discussion` / `complete-directive` comment-posters to `--body-file` (the dir-mode injection-safe bar). EI-6 of the hook-enforcement hardening directive. (#504)
