- The six body-authoring commands (`/file-issue`, `/file-directive`, `/revise-directive`, `/sync-pr`, `/work-on`, `/consume-initiative`) and the `planner` agent now carry an advisory **claim-ordering step** (SPEC §1.11 L2/L5) beside the existing citation step: each descriptive fact in a durable body is taken in L5's order — **delete** it (the default), else reduce it to a **pointer**, and only a residue that must appear as a figure is **rendered** via `scripts/ghjig_evidence.sh evidence` — so the render helper is reachable from the surfaces an author is actually reading, as the last rung and never the opening move. Advisory like the citation step: it gates nothing and changes no reviewer verdict; a structural smoke arm (§186) re-derives the carrier set at runtime and reds if a body-authoring surface lacks the step. (#728)
