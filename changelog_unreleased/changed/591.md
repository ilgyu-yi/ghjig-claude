- The `permissions.allow` classifier exception for the exact `/ship` merge command is now injected into every onboarded target via `settings.injected.json` (previously dogfood-only), so a target gets the classifier exception paired with its already-injected `merge-review` gate and unattended `/ship` can reach merge instead of parking. (#591)
