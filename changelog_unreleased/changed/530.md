- Plan review is now an adversarial contest: the planner produces one base plan, two independent `plan-challenger` agents attack it on distinct auto-selected axes (parallel + worktree-isolated), and `plan-reviewer` judges the three candidates — replacing the old model where the planner graded its own self-authored alternatives. (#530)
