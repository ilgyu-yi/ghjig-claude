- `/file-review <pr>` (SPEC §5.29) — a producer command that runs `code-reviewer` on a PR and materializes its verdict as a first-class, `commit_id`-pinned GitHub review submitted via REST (`gh api …/pulls/<n>/reviews`, not `gh pr review`, so a concurrent push cannot rebind an approve to an unreviewed head). Ownership-branches: another author's PR → native `APPROVE`/`REQUEST_CHANGES`; the authed user's own PR → a `COMMENT` review carrying the machine-readable marker `<!-- file-review verdict=<approve|block> head=<HEAD_SHA> reviewer=code-reviewer -->` (GitHub 422s a self approve/request-changes). Maps `ship`→approve and `ship after fix`/`block`→block; an unconfirmed/unresolvable head posts nothing and audits `invalid` rather than a block on a head it couldn't confirm. Body is passed via a written, `@mention`-neutralized temp file; `<pr>` is validated and the PR must target the origin repo (cross-repo confirmed in `attended`, refused in `unattended`). Producer-only — it changes no merge gate; the #586 merge-review gate will consume it. (#585)
