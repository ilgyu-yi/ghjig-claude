- New `scripts/ghjig_evidence.sh` — the sanctioned authoring path for descriptive facts in durable bodies (Directive #637 L2): an `evidence` face that executes a command and emits its command + verbatim output + head-sha pin as one fenced block, and a `quote` face that verifies a span resolves verbatim at its attributed path before emitting, refusing otherwise. (#716)
