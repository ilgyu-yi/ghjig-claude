- ADR 0007 records the substrate for Directive #639's per-change attributable reject count: the `/file-review` materialized GitHub review-object marker (extended in a later unit to carry a reason class), chosen because it is the only candidate durable across a fresh clone — the audit-log-based alternatives all live under git-excluded `.claude/ghjig-state/` / `.claude/audit/` and vanish in a fresh clone — while being head-pinned (per-change attribution) and independently degradable (the degrade record lands in the audit log, a separate channel). (#690)
