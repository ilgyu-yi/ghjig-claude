- `claude-eng <dir>` now runs a launch-time binding-health check: if an injected target's `.claude/eng-shell-root` binding is missing or dangling, it warns (non-blocking) before starting the session and names the repair (`scripts/register.sh`). This surfaces the broken-binding silent-no-op that the #318-removed SessionStart banner structurally could not detect (the hook runs through the broken link). (#334)
