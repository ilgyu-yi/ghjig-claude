- Added an opt-in Code-phase **implementer** subagent and the `/implement <issue#>` command (Directive #477, EI-1): the implementer authors the Code phase from an explicit manifest (the Plan + the failing test + the named relevant files), iterates in its own ephemeral context, and returns only a structured result (commit/diff + plan-deviations + discoveries) — so within-Execution churn never enters the main loop, while the manifest is the selective-injection dual. It is **not** the default — nothing auto-routes the Code phase to it (a smoke lock guards this), pending the Directive's A/B measurement gate; invoke it explicitly via `/implement` (#486)
