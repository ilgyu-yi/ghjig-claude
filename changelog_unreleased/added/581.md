- A Python readability/language-idiom rubric (`.claude/rubrics/python.md`) — the second language on the #546 per-language mechanism. `code-reviewer` conditionally reads it when a diff contains Python and applies it as advisory idiom-notes (never a block): EAFP/duck typing, comprehensions & generators, context managers, idiomatic iteration, `dataclass`/`NamedTuple`/`Enum` structured data, type hints as contracts, and the `type-by-attribute-combination` design smell (dispatch on the explicit discriminator). PEP 8 / unused-imports / mutable-default / bare-`except` are noted as delegated to ruff/flake8; a Python deterministic checker script is deferred (no Python code / no pinned linter in this repo). (#581)
