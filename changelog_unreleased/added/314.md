- Per-project ephemeral state: the audit log and `.claude/state` caches now resolve via `eng_state_dir()` to `$CLAUDE_PROJECT_DIR/.claude/eng-state/` in hook context (else the legacy shared path), so two projects on the same shared codebase have mutually-invisible audit/caches. The scope-guard registry move is a separate follow-up. (#314)
