- PostToolUse now nudges toward a targeted `Read --offset/--limit` (or `explorer` delegation) when a whole file is read above a line threshold — a positive, non-blocking narrowing affordance; see the new SPEC §1.8 inventory of in-session narrowing levers. (#389)
