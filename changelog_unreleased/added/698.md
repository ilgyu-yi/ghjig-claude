- The two authoring agent contracts gain the skeptic-probe obligation: a byte-identical clause in `doc-writer` (Responsibilities) and `implementer` (Engineering norms) obliges each, before returning, to attempt one violation of what it just asserted, confirm it fails, and ship the probe (command + output) as the SPEC §1.10(d) evidence — the bare assertion without its probe is not written. The implementer's churn-discard carries the matching carve-out — the one probe's command+output pair and its red are return content, carried in (c), while the probe's iterations remain churn — and the CLAUDE.md evidence-discipline digest gains the same-change self-claim clause as a thin pointer to §1.10. (#698)
