- A new `merge-completeness` advisory arm on `gh pr merge` (immediately after `merge-attestation`, same entry grep): on a `feat`/`fix` PR whose merge diff touches zero source files (non-empty file list, every path test/doc per the reused `.shellsecretignore` classifier) it emits an `audit_log warn merge-completeness` record plus a one-line stderr notice, then allows — the positive-completeness face of the merge-attestation block. It reuses the hardened merge argv parser and a single bounded `gh pr view --json headRefName,title,files` round-trip, and NEVER blocks: fail-open throughout (PR unresolvable / gh down / empty diff / helper miss → no warn, allow). (#548)
