- Ship the tier-3 `check-ssot-home` CI gate into the dir-mode substrate: enforces docs-thin-pointer parity (internal smoke §91) and SPEC-as-content-home for bound targets, catching the missing-SPEC case `check-toc.yml` structurally cannot, and closing the shell-vs-target enforcement asymmetry (Directive #626). (#627)
