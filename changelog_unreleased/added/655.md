- SPEC §6.0 P4's honest-mistake/hostile-input classification is completed: three `/file-review` wrapper reject arms with a message surface — `staging-absent`, `mtime-unresolvable` (both sites), `mtime-future` — were in neither bucket and now carry an em-dash recovery clause naming their live environment fix, classified in SPEC §5.29 as honest-mistake arms (the #647 `now-malformed` precedent). The §5.29 honest-mistake set is now a single source that the `§148h-p4` smoke lock derives from (no hardcoded twin), the lock iterates per call site (not per arm name), carries an anti-vacuity sentinel floor so a de-backticked enumeration cannot vacuously pass, and §6.0 P4's classification sentence is phrase-locked. The over-length twin sites of `mtime-malformed`/`now-malformed` are exempted in the SSOT, not only in test code. (#655)
