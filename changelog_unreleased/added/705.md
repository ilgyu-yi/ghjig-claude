SPEC §1.10's authoring discipline is now delivered where a durable-artifact body is written, not only at the two filing paths that already carried it: the surfaces that write an Issue, Directive, or PR body carry one byte-identical clause anchored on the `gh` call that writes the body, `planner` carries the obligations that bind an author who stages no file, and the skeletons a shell step composes a body from carry the hint as a head comment. `.claude/CLAUDE.md`'s Evidence-discipline statement contracts to a pointer whose scope is no longer narrowed by its enclosing heading and whose clauses are labelled (a)–(d) so the reader's `part (a)` report can be read against it.
