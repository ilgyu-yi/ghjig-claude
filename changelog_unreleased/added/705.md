- SPEC §1.10's authoring discipline is now delivered where a durable-artifact body is written, not only at the two filing paths that already carried it: one byte-identical clause — anchored on the `gh` call that writes the body, carrying the fail-open surfacing obligation so a relayed report cannot read as "clean" when the reader declined to scan — lands at `/work-on`, `/sync-pr`, `/revise-directive`, `/consume-initiative` and, as a head comment, at the `issue`/`directive`/`pr_body` skeletons a shell step composes a body from; `planner` carries its own line stating which parts bind an author who stages no file and why the lexical reader cannot run there. `.claude/CLAUDE.md`'s evidence-discipline statement contracts to a pointer whose scope is no longer narrowed by its enclosing heading and whose clauses are labelled (a)–(d), so the reader's `part (a)` report can be read against the rule as delivered. (#705)
