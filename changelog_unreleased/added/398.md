- Closed the friction-observability loop: a once-per-session, non-blocking, TTL-gated SessionStart advisory (SPEC §6.5(d)) now surfaces accumulated friction (escape/promotion clusters + a new additive `unattended-park` audit record bridging overnight park frequency into `audit.jsonl`) as a one-line pointer to `/audit` — so the shell sees its own friction without a human running a script (#398).
