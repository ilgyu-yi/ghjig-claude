- Audit-log consumers for the §6.0 P3 advisory→hook→narrowed migration (Directive #356 signals 2/4/5): `scripts/narrowing_candidates.sh` surfaces escape-clustering over LIVE records (same category/reason across distinct days) as gates that may warrant narrowing, and `scripts/promotion_candidates.sh` aggregates the reviewer-reject reason-class trail (#361) into advisory→hook promotion candidates above a threshold. Both are read-only and surfacing-only — they list candidates; the narrow/promote decision stays human/reviewer judgment. SPEC §6.0 P3 + MISSION "The mechanism" now state the dual-positive-channel concept (a block's in-band message is its immediate positive face; the audit line is a deferred positive face, latent until a consumer is wired). (#363)
