- SPEC §1.10 gains part (c), the claim half-life discipline: a durable artifact carries no claim that expires before the artifact does — state the rule, never a census; write a number only when it is the claim, and then pin it to the tree state it measured; a self-referential measurement pins its corpus or excludes itself; review archaeology lives in the review record; and the remedy for a stale volatile claim is removal or pinning, never a refresh. `code-reviewer` files that remedy by default, `finding-judge` refuses instance-correction on a claim-class finding whose axis key already drew a confirmed finding in a resolved prior round, and the always-injected CLAUDE.md points at the contract. (#680)
