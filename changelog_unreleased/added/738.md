- Two evidence-consuming PreToolUse gates land the near-free half of Directive #692: `gh issue edit --remove-label status:proposed` blocks without a fresh trusted comment whose first line is `<!-- activation-verdict: pass -->` (freshness derived from one GraphQL round-trip — `lastEditedAt` / last label event / last-100 comments — because live measurement showed the REST `updatedAt` proxy reads the shell's own flows as stale), and `gh issue close --reason completed` on a `directive` Issue blocks without the `## Directive Completion` closing comment (existence-only; non-directive Issues unaffected). Both fail closed on any lookup failure — the `merge-review` posture, including a `safe_source` miss — with distinct evidence-absence (`/activate` / `/complete-directive`) and lookup-failure messages, and are §7-escapable as `activation-evidence` / `completion-evidence`. `/activate`'s pass path reorders to comment-then-single-edit so the evidence exists when the flip fires. (#738)
