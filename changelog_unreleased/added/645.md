- Add the `finding-judge` subagent (SPEC §4.13) — the code-side counterpart of `plan-reviewer`: it judges `code-reviewer`'s findings before the author acts, ruling each `confirmed`/`refuted` with the command and mode it ran, assigning `fix-now`/`defer-to-issue`/`drop`, and holding every `fix-now` item to a three-rule anti-swing contract over a closed both-ends axis menu. The judged list is posted durably before the fix is written, which gives the review-response phase the manifest it has never had. It judges, it never authors the fix; it is not a vote and not a merge gate, and it fails open loudly rather than parking. (#645)
