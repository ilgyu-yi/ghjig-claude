- SPEC §1.10 states the author-side evidence discipline for a durable-artifact body: a quotation resolves in the **file** it is attributed to — a string that exists elsewhere in the tree but not at the attributed site is a site-mismatch, the defect an existence-anywhere check passes — while a `:NN` beside the path is informational, since line numbers drift; and a corpus-quantified claim carries the literal command and its literal output, never a prose summary of either, neither broader nor narrower than that output. `/file-issue` surfaces the born-advisory `scripts/lint_citations.sh` report to the author before the reviewer gate; it reads the lexical half of the first part only, exits 0 always, never gates, and a clean report is not verification. (#676)
