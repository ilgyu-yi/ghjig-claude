- The artifact-authoring skills (`/file-issue`, `/file-directive`, `/work-on`, `/ship`, `/complete-directive`) now each carry a `## Work language` note instructing the agent to recast context and author their durable artifact in the configured work language (`resolve_work_lang`, SPEC §5.7.2) — completing the communication-vs-work-language separation of Directive #322. (#327)
