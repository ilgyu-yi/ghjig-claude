- `code-reviewer` and `finding-judge` now carry a bounded **execution scope** for the reproductions they run: **write-confinement, not read-confinement** — ambient-tree reads stay open (extracting blobs, reading fixtures, sourcing the harness preamble read-only), but the run is scratch-confined (its cwd and every write land inside the agent's own scratch directory), and "execution against the ambient worktree" is the forbidden shape. PR #649's extract-into-scratch method is named the canonical permitted example. SPEC §4.5 (and §4.13 by reference) states the contract, born-advisory per §6.0 P3; the hardening trigger is recorded as blocked on an absent observable (nothing yet attributes an executed command to the subagent that ran it), with the unblocking observable named for a later Issue. (#650)
