- The reviewer-reject trail gains its cross-occurrence consumer (Directive #688 boundary item 3): `scripts/axis_recurrence.sh` groups reject records by (issue, reason-class) across reviewer categories and, when the same target has drawn the same reason-class a second time, emits one observe-only line carrying `change the form, not the instance` — a first occurrence emits nothing. Pair lines carry the category set and the occurrence timestamp range so duplicate emissions stay legible; malformed audit lines are skipped per-line rather than blinding the report; the reader fails open and always exits 0. The report's own header states the calibration limit (reason-class is not a calibrated axis key), no enforcement face consumes the signal, and the §6.5(d) SessionStart wiring is deliberately deferred as the named follow-up. (#697)
