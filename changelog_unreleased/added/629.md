- `build_toc.sh` now distinguishes a marker-less / anchor-link SPEC ToC (exit 3) from a stale marker ToC (exit 1) and a corrupt half-marker (exit 4), and adds a transactional, content-preserving `--migrate` that converts a legacy anchor-link ToC to the line-number marker form; `check-toc.yml` maps each to a fix-naming message (Directive #626). (#629)
