- Added the tier-3 server-side enforcement authority (`scripts/install_branch_protection.sh`): a capability-adaptive escalation ladder that SETs default-branch protection where the caller is admin — a named per-repo ruleset `ghjig-tier3` (updated by id, zero-clobber, default-rename-safe) or, where rulesets are unsupported, a classic branch-protection floor-merge PUT (never a destructive replace) — verifies the five facets for any actor via `GET rules/branches ∪ GET branches/protection` (`configured`/`partial`/`absent`/`unreadable`, fail-closed honest), and degrades to report+prescribe without admin. `/onboard` now surfaces the tier-3 state and prescribes (never auto-mutates); the bypass surface (`bypass_actors`/`enforce_admins`) is reported, never forced. This completes SPEC §6.7's three-tier model — tiers 1–2 advise, tier 3 is the non-bypassable authority. (#613)
