- New `/changelog` skill authors a changelog fragment (or applies `skip-changelog`) in-flow, and `/ship` now gates the ready transition on the fragment-or-skip contract so it is satisfied before CI rather than after a red check. (#346)
