- New `scripts/ghjig_rounds.sh` — the rounds instrument (Directive #637 item 7): it reports the judged-round count per PR and the sequence as a trend, derived through the judged-list code home rather than asserted, with a merge-ordered `--recent <M>` window. It reports and gates nothing. (#721)
