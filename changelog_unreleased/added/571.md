- The `plan-reviewer` contest now carries a judge-side **dismissal evidence-burden** guard — the mirror of the challenger-side `fake-diff` / `minimalism-by-deferral` guards applied to the judge's own verdict: an `A-stands` against a domination claim is valid only when the judge *refutes* the challenger's specific weakness with evidence (scaled to its claimed magnitude), never merely *outweighs* it with incumbent advocacy. It closes the reclassification-to-dodge and unevidenced-down-rate escape hatches and fires only on a domination claim, so a large weakness can no longer be buried as an "acceptable trade-off" while benign incumbent-presumption is preserved. (#571)
