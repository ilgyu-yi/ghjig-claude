- New **local git-hook enforcement tier**: committed `.githooks/` adapters (pre-commit, pre-push, commit-msg) that reuse the existing helper checks (protected-branch, secret scan, commit-format) extend the working-flow to ANY local git op — human/terminal/script, not just the Claude Code agent. Activate per-clone with `scripts/install_git_hooks.sh` (repo-local `core.hooksPath`; `--check`/`--uninstall`); `/onboard` installs it and a SessionStart drift check nudges when it is inert. Advice tier (folds to `--no-verify`); server-side authority is a deferred follow-up. (#604)
