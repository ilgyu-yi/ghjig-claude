- A third evidence-consuming PreToolUse gate lands Directive #692's Layer-1 item 4: `gh pr ready` blocks unless the PR diff adds a changelog fragment whose stem is in the allow-set (PR number ∪ `closingIssuesReferences`) or the PR carries the `skip-changelog` label — the label door checked first, so hook and CI agree on both doors. The added-in-diff predicate requires a path to appear in both the `diff --git` header grep and the `+++ b/` grep of one fetched `gh pr diff --patch`, so a content-forged `+++` line cannot mint evidence; presence-only, fragment shape stays CI's (§18.5). Fails closed on every lookup failure (transport kept separate from grep-no-match, the #553 E3 split) with a three-way block message — `/changelog` remedy, stem-outside-allow-set rename remedy, lookup-failure re-run remedy — and is §7-escapable as `changelog-evidence`; `gh pr ready --undo` stays ungated. (#742)
