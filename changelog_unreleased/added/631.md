- `/onboard` now reports SPEC ToC-format (marker line-number vs marker-less / corrupt, a format check — not freshness, which stays the `check-toc.yml` CI gate) and `docs/*.md` thin-pointer state as gh-free fact lines, and the `build_toc.sh --migrate` guidance now states its numbered-heading precondition instead of implying it converts any ToC (Directive #626). (#631)
</content>
</invoke>
