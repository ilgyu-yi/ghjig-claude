- Reviewer contest now runs a mandatory-invariant preservation gate before the axis contest (`plan-reviewer`) and the evidence judgment (`activation-reviewer`): a candidate that defers, weakens, or measurement-gates a declared invariant is disqualified regardless of axis score, `/work-on` threads a dispatch-derived `Mandatory invariants` manifest, and an empty manifest resolves by affirmative attestation rather than a hard block. (#569)
