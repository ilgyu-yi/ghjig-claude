- An advisory readability / language-idiom review axis: a per-language rubric (`.claude/rubrics/bash.md`) conditionally read into `code-reviewer` when the diff contains that language, plus the born-advisory `scripts/lint_bash_idioms.sh` deterministic checker (reuses the pinned shellcheck at a broader-than-gate severity, adds only the `safe_source` and `git add -A/-u` project-policy greps). Advisory by construction — never a block, never wired into the fail-closed CI lint gate. (#546)
