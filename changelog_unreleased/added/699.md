- The `/implement` manifest contract gains the anti-enumeration rules (Directive #688 boundary item 2), a byte-identical sentence pair across SPEC §5.28 and `.claude/commands/implement.md`: the relevant-file list carries the derivation command that produced it or an explicit non-exhaustive label (the assembler treats an unlabeled list as an incomplete manifest), and an invariant and an example-list never co-locate in one handoff block — each travels in its own labeled slot, and the assembler flags a co-located block at assembly rather than passing it through. SPEC §5.28 alone carries the two-sided statement (a list with its derivation command or label passes assembly; a co-locating block is flagged at assembly), and §4.12's structured-return contract records the #698 carve-out in summary voice — the skeptic-probe's command+output pair and its red ride in (c) discoveries while the probe's iterations remain churn. (#699)
