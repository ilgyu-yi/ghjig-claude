- The activation arm gains the same neutral triage layer the code arm has had since #645: `finding-judge` generalizes to the one adjudication role behind any adversarial reviewer arm (no new subsystem), dispatched from `/activate` step 3.5 on every findings-carrying verdict — `pass` included — with the judged list durable on the Issue before the verdict is applied. On the Issue substrate the artifact pin is a bare-hex sha256 body-prefix riding the existing validator, enforced twice: issue-mode `post` recomputes the live-body hash and refuses a stale post, and the next round's judge rules a moved body `prior-round: unresolved — body advanced` (the #723 doctrine with no new grammar). The below-bar disposition #705 improvised in prose ("carried to the PR body, not fixed here") becomes the judged `defer-to-issue` outcome. The reviewer's `pass`/`revise`/`reject` grammar, the filer matrix, and the judge's no-verdict-token posture are unchanged. (#707)
